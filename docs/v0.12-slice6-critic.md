# v0.12 slice 6 — critic review

Slice 6 shipped as TWO commits (split from a single bundled staging
by simplifier-18cy after builder-dfb2 timed out at 40m mid-stage):
- `9599e5d` — `fix(rpc-stdin): drop raw-LF rejection; keep CR-only tripwire`
- `60a175a` — `feat(commands): /conductor send + live RPC integration tests`

HEAD: `60a175a`.

## Review

### Trajectory remeasure (item 1)
- ✓ Correct: `env -u CONDUCTOR_SUBAGENT npm test` at HEAD `60a175a` → 1274 tests / 1263 pass / 11 skipped / 0 fail / 11.7s. Matches builder claim.

### Fix-commit `9599e5d` audit (item 2)
- ✓ Correct: `JSON.stringify({m: "a\nb\rc"})` round-trip confirmed via `node -e` — output contains `"\\n"` (escaped) and `"\\r"` (escaped); raw `\n`/`\r` absent. The fix's rationale holds.
- ✓ Correct: **mutation kill** — re-introducing the LF rejection (`return value.includes("\n") || value.includes("\r")` at `src/rpc-stdin.ts:52`) reds `tests/rpc-stdin.test.ts:248` "multi-line strings in payload values are escaped by JSON.stringify and accepted (slice 6 fix)". Restored clean (`git status` empty).
- ✓ Correct: commit body cites slice 6 live tests as the witness ("Slice 6's live integration tests exposed it. ... Caught by: slice 6 live integration tests"). Conventional Commits subject form correct.
- ✓ Correct: existing slice-2 invariants preserved verbatim — diffed `EPIPE` and `destroy(reason)` test bodies pre/post; both byte-identical. Helper `findRawLineSeparator` → `findRawCr` rename + comment rewrites + 1 test inverted (`rejects embedded newlines` → `multi-line accepted`); CR-still-rejected pin retained at `tests/rpc-stdin.test.ts` framing case.

### Feat-commit `60a175a` slash command teeth (item 3)
- ✓ Correct: tests at `tests/conductor-send-command.test.ts:22-79` assert on `out.behavior` directly (not `assert.doesNotThrow`).
- ✓ Correct: **mutation 1 kill** — flipping default `let behavior: StreamingBehavior = "auto"` → `"steer"` at `src/commands.ts:434` reds `parseSendCommand: bare <id> <msg> → streaming_behavior=auto` at line 22 with `expected: 'auto', actual: 'steer'`. Restored.
- ✓ Correct: **mutation 2 kill** — dropping `"--steer": "steer"` from `SEND_FLAGS` at `src/commands.ts:401` reds 4 tests including the named `--steer flag → streaming_behavior=steer`. Restored.
- ⚠ Note: **mutation 3 (drop missing-agent-id branch) did NOT red.** Commenting out the `if (trimmed.length === 0)` branch at `src/commands.ts:424-426` left all 12 tests passing. Cause: when the agent-id branch is gone, an empty input flows through to the missing-message branch (`return { kind: "error", message: missing message. ${SEND_USAGE} }` at `src/commands.ts:459`). The error message interpolates `SEND_USAGE` ("usage: /conductor send <agent-id> ..."), so `assert.match(out.message, /agent-id/)` and `assert.match(out.message, /usage/i)` at `tests/conductor-send-command.test.ts:60-61` both still match via the usage string. The production behavior (specific "missing agent-id" prefix vs generic "missing message") is correct; the assertions are too loose to catch its removal. Suggested follow-up: tighten to `assert.match(out.message, /^missing agent-id\./)`. **Not blocking** — production code is correct; coverage gap is in the assertion.

### Live integration tests — gating discipline (item 4)
- ✓ Correct: 11 skipped without `CONDUCTOR_LIVE_TESTS=1`. 5 of those are slice 6's new live tests, all annotated with `# set CONDUCTOR_LIVE_TESTS=1 to enable (uses real pi subprocess + AWS creds)`. The other 6 are pre-existing (`tests/spawn-integration.test.ts`).
- ⚠ Note: **did not run with `CONDUCTOR_LIVE_TESTS=1`.** This critic process is itself a pi sub-agent and `CONDUCTOR_SUBAGENT=1` is set (gate at `src/extension.ts: maybeAutoRunGc` skips on this). Running live tests inside a sub-agent risks recursive/contention failures (sub-agent spawning real pi to test sub-agent spawning); the critic persona's own subprocess context cannot fairly exercise the integration suite. Recommend the parent conductor run `env -u CONDUCTOR_SUBAGENT CONDUCTOR_LIVE_TESTS=1 npm test` before slice 7. **Not blocking** — gating discipline is verified; live execution is pi-version-drift-monitoring per the brief's anti-goals.

### Live test fixture discipline (item 5)
- ⚠ Note: each of the 4 `integration-rpc-spawn.test.ts` cases calls `spawnSteerableInspector` independently (`tests/integration-rpc-spawn.test.ts:119, 172, 241, 302`); no shared `before`/`after` hooks. Header comment at line 7 explicitly documents this: *"single-spawn pattern across cases is NOT possible — each case exercises a distinct lifecycle"* (steer mid-turn, follow_up, forceTerminate, initial-prompt-bump are all destructive of the spawned run). Design decision, not regression. Wall-clock-<60s claim cannot be verified without `CONDUCTOR_LIVE_TESTS=1`. If wall-clock proves a problem in CI later, refactor candidates: share the inspector for cases 1+3 (initial-prompt + follow_up are non-destructive of session), keep cases 2+4 (steer + forceTerminate) standalone.

### Overlay 's' keybinding regression (item 6)
- ✓ Correct: `git diff 11089ef..60a175a -- src/focused-overlay-factory.ts src/prompt-and-send.ts` empty. The 's' keybinding routes through `promptAndSendToRun` (`src/focused-overlay-factory.ts:82` calls `deps.promptAndSendToRun(id)`) and the helper itself (`src/prompt-and-send.ts`) is unchanged across the slice.
- ⚠ Note: **plan reference is stale.** Plan §"Slice 6" line 281 says *"the `s` keybinding in `src/focused-stream-overlay.ts:228` requires no code change"*. That filename doesn't exist at HEAD; the keybinding now lives in `src/focused-overlay-factory.ts:82` (per the focused-overlay-redesign predecessor). Functional invariant (no diff to overlay 's' wiring) holds; only the plan reference drifted. Worth fixing in the slice 7 closure prose pass, not blocking slice 6.

### Slice 7 territory leak (item 7)
- ✓ Correct: `git diff 11089ef..60a175a -- src/conductor-prompt.ts README.md PRD.md` empty. No leak into slice 7 territory.

### CONTRIBUTING.md v0.12 dogfood (item 8)
- ✓ Correct: appended at `CONTRIBUTING.md:106-136` (after existing line 103). New section header `## v0.12 dogfood (steerable sub-agents)`. No modifications to lines 1–103.

### dist regeneration (item 9)
- ✓ Correct: `git show --stat 9599e5d` → 4 files (`dist/index.js`, `dist/index.js.map`, `src/rpc-stdin.ts`, `tests/rpc-stdin.test.ts`). The dist regen tracks only `src/rpc-stdin.ts`'s changes (1 helper rename, 1 invariant flip); no unrelated drift. Pre-commit hook auto-rebuild as documented in `CONTRIBUTING.md`.

### Novel verification (item 10b — existing-test regression check on the 128-line test diff)
- ✓ Correct: diffed `RpcStdinQueue: enqueue rejects on EPIPE with cause-tagged error; queued writes after still succeed when pipe is alive again` and `RpcStdinQueue: destroy(reason) rejects all pending writes with the supplied reason` test bodies between `11089ef:tests/rpc-stdin.test.ts` and `9599e5d:tests/rpc-stdin.test.ts`. Both byte-identical. Pre-existing slice-2 contract invariants (back-pressure ordering, EPIPE non-latching, destroy-rejects-pending) are not silently weakened by the fix. Helper rename `findRawLineSeparator` → `findRawCr` is internal; both pre/post tests construct payloads via the exported `RpcStdinQueue` API only, so no test reaches into helper-naming-coupled internals.

## Verdict

**APPROVE.**

All 10 review items pass. Two `⚠ Note` items (loose assertions on missing-agent-id branch; stale plan reference to `focused-stream-overlay.ts`); both are coverage/documentation issues, not correctness issues. One mutation in 3 attempted on the slash command teeth did not red, but the cause is assertion looseness on a production-code-correct branch, not a missing implementation.

Mutation kill score: **3 of 4 attempted** (LF rejection, default-`auto`, `--steer` removal each killed exactly the test the brief named; missing-agent-id removal hit a coverage gap in the assertions). Per AGENTS.md "When you do find a bug, prefer one strong objection over a pile of weak ones" — the assertion looseness is one weak objection. Production behavior is correct.

Slice 7 (docs closure) clears for builder.

## Carry-forward Notes for slice 7

1. **Tighten missing-agent-id assertion** in `tests/conductor-send-command.test.ts:60-61` to `assert.match(out.message, /^missing agent-id\./)`. One-liner; could go in slice 7's commit or a tiny `chore(test)` slip.
2. **Update plan reference** at `docs/v0.12-steering-plan.md:281`: `src/focused-stream-overlay.ts:228` → `src/focused-overlay-factory.ts:82`. Pure docs.
3. **Run live integration tests outside sub-agent context** before merging slice 7. `env -u CONDUCTOR_SUBAGENT CONDUCTOR_LIVE_TESTS=1 npm test 2>&1 | tail -15` from the parent conductor's TUI. Per CONTRIBUTING.md's new v0.12 dogfood block.
