# v0.12 — Steering — Slice 3 critic review

Slice: `feat(rpc): subprocess wire-up + event dispatch + initial prompt + auto-cancel`
Commit: `394e69e` (vs baseline `21e6314`)
Plan: `docs/v0.12-steering-plan.md` §"Slice 3" lines 139–177
Design: `docs/v0.12-steering-design.md` §4.2 (dispatch table), §4.7 (deferred), §10 Q1 / plan §5 Q1 (immediate-write)

---

## Verifications I performed beyond builder's self-audit

1. **Baseline trajectory measured in a clean worktree at `21e6314`** —
   active count = **1207** (14 skipped). HEAD at `394e69e` measures
   **1217** active (14 skipped) — confirms the commit body's
   `1207 → 1217 (+10 active)` claim. (`git worktree add /tmp/pi-baseline-21e6314 21e6314` then `npm test`.)

2. **Mutation: `setImmediate(() => …enqueue)` wrap on the auto-cancel
   handler at `src/event-handler.ts:200`** — same-tick test +
   every-method coverage test + direct-invocation pin all red (3 fails).
   Restored, suite green. Same-tick discipline has teeth.

3. **Mutation: inserted `run.lastEventAt = Date.now()` into the new
   `response` branch at `src/event-handler.ts:60`** — slice-3 no-bump
   pin (`tests/event-handler-rpc.test.ts:88-91`) red. Restored, suite
   green. Slice-5 territory is genuinely guarded.

4. **`git diff 21e6314..394e69e -- src/event-handler.ts` audited for
   deletions** — zero `^-` lines outside the diff header. Existing
   `message_end` (line 86) and `tool_result_end` (line 120) bumps
   are byte-identical to baseline. Print-mode regression contract
   preserved at the file-diff level, not just at the test level.

5. **`spawnRun` ordering audit** at `src/runs.ts:1048`–`:1093`:
   `spawn()` → `attachSpawnedProc()` → `stampSpawnStreamingMode()` →
   `if (steerable)` { `new RpcStdinQueue(proc.stdin)` →
   assign `run.rpcStdinQueue` → fire-and-forget enqueue }. Sequential
   inside one `if` block; the enqueue cannot fire before the queue is
   attached. No misordering.

---

## Review

### ✓ Correct

- **Auto-cancel synchronous-tick discipline** —
  `src/event-handler.ts:185-208` (`handleExtensionUiRequest`). Direct
  `void run.rpcStdinQueue.enqueue(...).catch(...)` with no
  `setImmediate` / `queueMicrotask` / `await` defer. Mutation
  witness fires (3 named tests red).
  `tests/event-handler-rpc.test.ts:112-135` pins same-tick.

- **`routeRpcResponse` stub-only discipline** —
  `src/event-handler.ts:142-156`. Body is a single `return UPDATED;`
  plus a comment block sketching slice-4 behaviour. `grep -rn
  "pendingAcks" src/` returns 3 hits, all in comments
  (`event-handler.ts:135, 147, 150`). `grep -nE
  "clearTimeout|setTimeout" src/event-handler.ts` returns one comment
  hit at line 149 (slice-4 sketch). Zero live timer logic for ack
  timeouts.

- **Print-mode regression — byte-equivalence at file-diff level** —
  `git diff 21e6314..394e69e -- src/event-handler.ts | grep "^-"`
  returns only the `---` diff header, no deletions. Existing bumps at
  `src/event-handler.ts:86` (`message_end`) and `:120`
  (`tool_result_end`) are unchanged. Mutation witness against an
  injected bump in the new `response` branch confirms slice-5
  territory is guarded.

- **No territory leak into slices 4 / 5** — six surfaces audited:
  `src/runs.ts` keyword scan returns 3 comment hits citing future
  slices; `src/event-handler.ts: lastEventAt` returns 3 comment hits
  + 0 new write sites; `src/reconcile-startup.ts`, `src/tools.ts`,
  `src/watchdog.ts` all have **zero** lines changed in the slice-3
  diff. Slice 5's territory (`forceTerminate`, `RpcStdinQueue.destroy`,
  reconcile-orphan, watchdog bump asymmetry) is fully untouched.

- **Initial-prompt fire-and-forget swallow is scoped** —
  `src/runs.ts:1082-1093` is an inline `void queue.enqueue(...)
  .catch(...)` inside the `if (opts.steerable === true && proc.stdin)`
  block. NOT extracted into a generic helper. The other two `.catch`
  hits in `src/runs.ts` are pre-existing
  (`writeRecord/writeFinal` at line 1201, transcript-append at 1219).
  Slice 4's plan brief at `docs/v0.12-steering-plan.md:189`–`:195`
  explicitly takes ownership of the visible-to-LLM ack surface
  (`Run.pendingAcks` Map, 30s timeout, rejection reason). Acceptable.

- **`stampSpawnStreamingMode` purity** — `src/runs.ts:780-783`:
  two-line body, mutates `run.steerable` and `run.streamingMode`,
  no I/O / timer / event emit. Both branches pinned by
  `tests/runs-streaming-strategy.test.ts:284, 302`.

- **Conventional Commits compliance** — subject is byte-for-byte
  match to plan slice-3 title. Body cites design §4.2 (lines
  ~498–520, 401–570), plan §5 Q1, slice 3-of-7 breadcrumb, slice-4
  forward ref. Trajectory baseline `1207 → 1217` is correct.

- **`spawnRun` ordering** — `src/runs.ts:1048` `spawn()` →
  `:1064` `attachSpawnedProc()` → `:1070` `stampSpawnStreamingMode()`
  → `:1078`–`:1093` queue construction + assign + enqueue. Sequential
  inside one `if` block; no race window where the enqueue could fire
  before the queue is attached.

- **tsc clean** — `npx tsc --noEmit` returns no diagnostics.

- **Repo state** — `git status --short` shows only the 8 untracked
  v0.12 design-phase docs (intentional per AGENTS.md §11; commit at
  v0.12 close mirroring v0.11 pattern). No accidental dist-only or
  test-fixture leftovers.

### ⚠ Note (not blocking)

- **Dynamic-import type usage in `src/types.ts:467`** — builder
  declared this. `Run.rpcStdinQueue?:
  import("./rpc-stdin.ts").RpcStdinQueue` avoids pulling
  `node:stream` into `types.ts`. `grep -nE "rpc-stdin|node:stream"
  src/types.ts` confirms only the dynamic-import-type usage; no
  `node:stream` import added.

  Slice 4 will likely import `RpcStdinQueue` directly from
  `src/runs.ts` territory anyway (the `sendToRun` rewrite needs to
  call `enqueue`). At that point flipping to an eager import in
  `src/types.ts` is a one-line cleanup with no downstream impact.
  Slice 4 can fold it in or defer to slice 5 — either is fine.

- **Above-plan +10 trajectory** — plan §"Slice 3" line 167 estimated
  +4; commit body honestly explains the +6 are defensive multi-pins
  (every-method auto-cancel coverage, defensive no-queue path,
  print-mode regression in the streaming-strategy file). The brief
  pre-empts this as approve-able and the additions all pin slice-3's
  contract — no slice-4/5 territory leaked. Approve.

### ✗ Blocker

None.

---

## Verdict

**APPROVE.**

Slice 3 closes the wire-format-to-process pipeline cleanly. The
Risk-2 always-cancel-and-warn handler is synchronously enqueued and
mutation-tested. The `routeRpcResponse` stub is genuinely stubbed —
no `pendingAcks` field touched, no timer logic. Print-mode is
byte-equivalent at the diff level *and* mutation-tested at the
behavioural level. Slices 4 and 5's territory is provably untouched.
Initial-prompt fire-and-forget is scoped to one inline call; slice 4
explicitly owns the visible-to-LLM ack surface. Trajectory math is
honest (`1207 → 1217`, baseline confirmed by worktree). Conventional
Commits subject matches the plan title byte-for-byte.

Ready for slice 4 (`feat(send): streaming_behavior arg + per-call
steerable + ack envelope`).
