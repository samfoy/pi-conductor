# v0.12 — Slice 1 — Critic review

Slice commit: `3f73d9d` `feat(types): add steerable cascade and send-strategy resolvers`
Plan: `docs/v0.12-steering-plan.md` §"Slice 1" (lines 41–98)
Baseline: `1855d1f` (clean tree: 1184/1178/0 fail/6 skipped)
HEAD: `3f73d9d` (clean tree: 1212/1198/0 fail/14 skipped)

---

## Review

### W1 mutation-witness teeth (plan critic gate 1) — ✓ Correct

Mutation different from the builder's: replaced
`return run.steerable ?? defaultSteerable;` with
`return run.steerable ?? false;` (ignoring the default entirely).
`tests/steerable.test.ts:1:1241` (W1 witness) reds at:

```
✖ resolveSteerable: W1 — mutating the resolver to "run.steerable ?? !defaultSteerable" fails an assertion that pins the truth-table
  AssertionError: undefined falls through to default true
  false !== true
```

Witness has teeth. Restored via `git checkout src/steerable.ts`.

### W3 string-pin teeth (plan critic gate 3) — ✓ Correct

Builder (per commit message) mutated the paused reason. I mutated a
DIFFERENT one — `src/runs.ts:1278` `is queued and has not started`
→ `is QUEUED and has not started`. `npx tsx --test
tests/runs-streaming-strategy.test.ts` reds 2 named pins:

```
✖ resolveSendStrategy: queued (any) → rejected with "is queued and has not started yet"
✖ resolveSendStrategy: rejection reasons are character-pinned (W3 string witness — mutating one character fails)
```

Restored. W3 is character-precise across all 7 reject reasons.

### Out-of-scope grep (plan critic gate 2) — ✓ Correct

`grep -rn "steerable" src/` surfaces `src/runs.ts`, `src/config.ts`,
`src/steerable.ts`, `src/types.ts`. **`src/personas.ts` does NOT
surface.** `git diff 1855d1f..3f73d9d -- src/personas.ts` is empty.
Frontmatter parser untouched per oracle fix #1.

### Shape isomorphism with `resolveKillOnStall` (oracle gate 2 ADJUST) — ✓ Correct

`src/steerable.ts:42`:
```ts
export function resolveSteerable(run: Run, defaultSteerable: boolean): boolean {
  return run.steerable ?? defaultSteerable;
}
```
`src/watchdog.ts:287`:
```ts
export function resolveKillOnStall(run: Run, defaultKillOnStall: boolean): boolean {
  return run.killOnStall ?? defaultKillOnStall;
}
```

Both: `(run: Run, defaultX: boolean): boolean`, single `??` body,
exported, no cross-imports. Cascades stay isomorphic.

### Decision-matrix completeness (design §4.3 lines 545–605) — ✓ Correct

`tests/runs-streaming-strategy.test.ts` covers every cell:

- **running × rpc** × {auto, steer, follow_up, resume} — 4 named tests
  (lines 73, 78, 83, 88).
- **running × print|undefined** × 4 behaviors — looped (lines 100–110).
- **paused** × 4 behaviors × 3 modes — looped (lines 116–124).
- **queued** × 4 behaviors — looped (line 131).
- **terminal-with-session** × {auto, resume} × 4 statuses — looped
  (lines 141, 148).
- **terminal-with-session** × {steer, follow_up} — 2 named tests
  (lines 155, 162).
- **terminal-without-session** × {auto, resume} — looped (line 170).

7 reject reasons character-pinned at `:182`–`:265` (W3 witness).
Every assertion either pins the strategy `kind` or matches the
reason string — no overly-broad `kind === "rejected"` without
reason verification.

### Skip discipline on cascade scaffold — ✓ Correct

`npx tsx --test tests/steerable-spawn-cascade.test.ts`:
```
ℹ tests 8 — pass 0 — fail 0 — skipped 8
```

All 8 share `const PENDING = { skip: "pending slice 4 wiring
(steerable spawn-time cascade)" }` (`:49`). Test names cover
per-call > project > user > built-in precedence + short-circuit
+ shape invariants — matches design §4.1 cascade layer list.

### Type-shape audit (novel verification (a)) — ✓ Correct

- `Run.steerable?: boolean` at `src/types.ts:351` — ✓
- `Run.streamingMode?: "print" | "rpc"` at `:361` — ✓
- `RunRecord.steerable?` at `:525`, `RunRecord.streamingMode?` at
  `:531` — ✓
- `toRunRecord` at `:566`–`:567` copies both — ✓
- `ConductorConfig.defaultSteerable?` at top level (line 121, NOT
  nested under a sub-key like `watchdog.*`) — ✓
- `DEFAULT_CONFIG.defaultSteerable === false` at `:234` — ✓
- `mergeConfig` (`src/config.ts:103`–`:111`) merges
  `defaultSteerable` only; `defaultKillOnStall` JSON-merge path
  intentionally NOT added (forbid-adjacent-churn comment at `:106`).

### Existing-test regression check (novel verification (c)) — ✓ Correct

`tests/ensemble-send.test.ts:160` "rejects a running sub-agent
(busy)" gained:
- New precondition `streamingMode: "print"` (pins it as the
  non-steerable case post-v0.12).
- Assertion regex broadened from `/running|busy/i` to
  `/running|busy|not steerable/i`. Justified — the new reason
  string is `"...is not steerable... (Currently running; wait for
  it to finish...)"`, which still satisfies `/running/i` AND
  `/not steerable/i`. The status invariant `assert.equal(run.status,
  "running")` (no mutation) is preserved sharp.

Not a weakening — legitimate test evolution.

### Conventional Commits + plan compliance — ✓ Correct

- Subject: `feat(types): add steerable cascade and send-strategy
  resolvers` — exact match with plan §"Slice 1" title.
- Body cites design §4.1 + §4.3, plan §"Slice 1" lines 41–98,
  `src/watchdog.ts:287`, `docs/wdd.md`.
- Files touched (slice-1 list per plan):
  `src/{config.ts, runs.ts, steerable.ts, types.ts}` +
  `tests/{ensemble-send, personas, runs-streaming-strategy,
  steerable, steerable-spawn-cascade}.test.ts` + `dist/*`
  (pre-commit auto-rebuild). Matches plan exactly.
- Slice-2 territory NOT touched: `grep -nE "pendingAcks|
  RpcStdinQueue" src/runs.ts` returns 0; `buildPiArgs` unchanged
  (only docstrings reference `--mode rpc`).
- `npx tsc --noEmit` clean.

### ⚠ Note: Trajectory bookkeeping

The commit message + builder's evidence claim "+22 active tests
landed" against "pre-slice baseline 1197/1182/14". I measured both
trees cleanly:

- Baseline `1855d1f` (clean): **1184/1178/0/6**
- Slice-1 `3f73d9d` (clean): **1212/1198/0/14**
- Real deltas: **+28 total, +20 active, +8 scaffold-skip**

Plan §"Slice 1" target was `Suite: 1184 → ~1204 (+20: 4 resolver
+ 1 W1 + ~13 strategy-matrix + 2 ensemble-send + 1 personas-schema)`.
Actual landed: **+20 active**. Hits the slice-1 target on the
nose. Not a floor breach — the +65 cumulative floor in plan §4
applies across slices 1–7, not slice 1 alone.

The "+22 against 1197/1182/14" claim appears to be from an
intermediate state where slice-1 test files existed but src
hadn't landed yet (test file count rolled forward, src rolled
back). Bookkeeping discrepancy only — not a correctness issue.
Fix in the next slice's commit message: cite the clean
`1855d1f` baseline.

---

## Verdict

APPROVE.
