# Slice 5 critic review — `fix(watchdog,reconcile): RPC bump asymmetry + orphan reclassify + forceTerminate cleanup`

Reviewing commit `11089ef` against `docs/v0.12-steering-plan.md`
§"Slice 5" (lines 229–276), the design doc §4.4/§4.6/§4.7 pins, and
the slice-4 critic Note 2 carry-forward.

## Trajectory

`env -u CONDUCTOR_SUBAGENT npm test 2>&1 | tail -10` at `11089ef`:

```
ℹ tests 1257
ℹ pass 1251
ℹ fail 0
ℹ skipped 6
ℹ duration_ms 11181
```

Baseline `a21af42` → `11089ef`: 1232 → 1251 active (+19). Plan
target was +8 plus +1 carry-forward = +9; the additional +10 are
defensive regression / contract pins (W2(c), classifier print-mode +
dead-pid + undefined-mode regressions, executor print-mode
regression, executor ESRCH-race, executor RunStatus enum lock,
forceTerminate print-mode regression, forceTerminate
queue-undefined defensive). All approve-able as real, not
speculative — each pin maps to a documented contract or a witnessed
race. `npx tsc --noEmit` clean.

## Review

### Critic gate 1 — W2 mutation-witness teeth (PASS)

- ✓ Correct: `tests/event-handler-rpc.test.ts:28` imports
  `bumpOnRpcLine` directly from `src/event-handler.ts` (parallel-formula
  rule per `docs/wdd.md`; no inline re-derivation).
- ✓ Mutation A — `evtType !== "response"` (full negation) reds 5
  tests including W2(a), W2(b), W2(c), and both `applyEvent`
  integration tests. Restored.
- ✓ Mutation B — adding `|| evtType === "extension_ui_request"` to
  the bump branch reds W2(b) plus the `extension_ui_request`
  no-bump integration test (2 fail / 10 pass). Restored.
- W2(a) and W2(b) parallel the slice-1 W1 critic gate shape; teeth
  confirmed in both directions.

### Critic gate 2 — `classifyRecord` extension audit (PASS)

- ✓ `RunStatus` enum at `src/types.ts:239–247` is unchanged at 8
  members (`queued | running | paused | completed | failed |
  killed | timeout | hook_failed`). PRD `:524` D1 reuse pattern
  honored — no enum churn for v0.12.
- ✓ `errorMessage` prefix at `src/reconcile-startup.ts:526` is
  byte-exact `"orphaned: rpc-stream-detached"`. Symmetric with the
  v0.9.x `"orphaned: ..."` family.
- ✓ Mutation — replaced the prefix with `"orphaned:
  rpc-stream_detached"` (one-character swap, hyphen → underscore).
  Two byte-equality tests in
  `tests/reconcile-startup-rpc-orphan.test.ts:129,143,237` red.
  Restored.
- ✓ Family-glyph compatibility: `src/history.ts:103` uses
  `msg.startsWith("orphaned:")` — pattern-matches the family
  prefix, not a hardcoded suffix list. The new
  `rpc-stream-detached` shape renders the `⎘ orphaned:` glyph at
  `PRD.md:528` without further changes.

### Critic gate 3 — `forceTerminate` `pendingAcks` + W7 (PASS)

- ✓ Guard ordering: `src/runs.ts:1902` `isTerminal(run.status) →
  return` is the FIRST statement in `forceTerminate`, before the
  parentPid foreign-adoption defense, before the `pendingAcks`
  sweep at `src/runs.ts:1936–1948`, and before the
  `RpcStdinQueue.destroy` at `src/runs.ts:1951`. W7 idempotency
  guarantee preserved.
- ✓ `tests/forceTerminate.test.ts` "W7 idempotency — double-kill
  on RPC run with pendingAcks does not re-reject already-rejected
  promises" pins the contract.
- ✓ Mutation — commented out the `isTerminal` guard at `src/runs.ts:1902`.
  W7 idempotency test red (4 pass / 1 fail; only that test).
  Restored.
- ✓ Mutation — changed `destroy("force-terminate")` to
  `destroy("forced-terminate")` at `src/runs.ts:1951`. The
  cause-pin test "RPC in-flight stdin write rejects with cause
  'force-terminate' before SIGTERM" red (4 pass / 1 fail).
  Restored.

### Carry-forward closure (slice-4 critic Note 2) (PASS)

- ✓ Test exists at `tests/ensemble-spawn.test.ts:128` named
  `"ensemble_spawn: per-call steerable=undefined +
  config.defaultSteerable=true → run.steerable=true (production
  cascade path)"` — exact match to plan brief.
- ✓ Mutation — replaced `defaultValue: cfg.defaultSteerable ??
  false` with `defaultValue: false` at `src/tools.ts:291`. The new
  carry-forward test reds (2 pass / 1 fail; only the new case
  fails). Restored. Note 2 from slice 4 is closed with teeth.

### Slice-6/7 territory leak check (PASS)

- ✓ `git diff a21af42..11089ef -- src/commands.ts
  src/conductor-prompt.ts README.md PRD.md` produces zero output.
- ✓ `grep -rn "/conductor send\|conductor send" src/` returns
  zero hits in production code.
- Slice 6 owns `/conductor send` + live integration tests; slice
  7 owns the §10 prompt + closure docs. Neither leaked.

### `PostStartupReconcileDeps.signal` injection seam (NOTE)

- ⚠ Note: Builder added a new field on `PostStartupReconcileDeps`
  at `src/reconcile-startup.ts:352` plus an exported
  `defaultSignaler` at `src/reconcile-startup.ts:221`. Production
  callsite `src/index.ts:379` does NOT pass `signal:` — uses the
  default. No production behavior change.
- Slice 6 brief at `docs/v0.12-steering-plan.md:247` mentions a
  live integration test for the executor SIGTERM path but does
  not explicitly take ownership of the `defaultSignaler` symbol.
  The seam is testability-only; approve-able as a future-refactor
  Note rather than a Blocker.

### Side-effect audit on existing slice-3 test (PASS)

- ✓ Slice-3's `applyEvent: response line routes to
  routeRpcResponse stub and returns {kind: "updated"}` was
  correctly flipped to `applyEvent: response line on RPC run
  routes to routeRpcResponse and BUMPS lastEventAt (slice 5
  contract; supersedes slice 3 no-bump)`. Contract advanced, not
  weakened.

### Conventional Commits compliance (PASS)

- ✓ Subject `fix(watchdog,reconcile): RPC bump asymmetry + orphan
  reclassify + forceTerminate cleanup` matches plan slice-5 title
  exactly.
- ✓ Body cites design §4.4/§4.6/§4.7, plan §5 critic gates, PRD
  :524 D1, PRD :528, and includes the slice-5-of-7 breadcrumb +
  the M-bordering-XL split-on-demand decision (plan §3 + §6
  permitted).

## Verdict

APPROVE.

All three required critic gates have teeth verified by independent
mutation tests. The slice-4 carry-forward Note 2 is closed with a
named test that reds exactly when the production cascade path is
broken. Zero leak into slice-6/7 territory. The new `signal`
injection seam is test-only and does not change production
behavior — Note for future cleanup, not a Blocker.

Test trajectory `+19` exceeds plan target `+9` because of
defensive contract pins (RunStatus enum lock, ESRCH-race,
print-mode short-circuit regressions, queue-undefined defensive
path). Per the conductor's anti-goals, defensive regression pins
are approve-able if real, not speculative; spot-checks against the
ESRCH-race and print-mode regressions confirm they map to
documented contracts.

Builder may proceed to slice 6.
