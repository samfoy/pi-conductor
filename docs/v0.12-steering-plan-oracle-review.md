# v0.12 — Steering — Oracle gate review on the **plan**

Gate review of `docs/v0.12-steering-plan.md` (493 lines, 7 slices) against
the locked design (3 revision-log entries), the two prior oracle gates,
the inspector map, and PRD precedent. Slice-readiness, not redesign.
Loop iteration 1 of 3 per AGENTS.md §11.

---

## 1. Verdict

**APPROVE WITH FIXES.**

All four user locks (Q1/Q2/Q6/Q9) honored; all four oracle fixes applied.
Slice ordering is coherent and strictly serial. Three small ADJUSTs:

1. **Slice-1 ↔ slice-4 cascade-wireup contradiction** — slice 1 says
   "wired in slice 3" (`plan.md:52`); slice 4 + §4 table say slice 4.
2. **Trajectory math inflated by ~10–15** — the 8 cascade cases are
   double-counted across slice 1 (+26) and slice 4 (+16); slice 5's
   claimed +15 maps to ~8 enumerated acceptance cases.
3. **Plan §5 Q1 silently overrides design §10 Q1's default** — picks (c)
   immediate-write vs design's (a) first-stdout-line. Defensible but
   should be flagged as a deliberate revision, not buried as "Resolved."

Editorial; none structural. Ready for slice-1 builder after the ADJUST.

---

## 2. Slice ordering and dependency check

Strict serial chain (`plan.md:30-37`): 1 → 2 → 3 → 4 → 5 → 6 → 7. Audited
each forward dependency:

- **Slice 1 → none.** Pure types + pure resolvers. No subprocess plumbing.
  `validateSendable` rewrite as shim of `resolveSendStrategy(run, "auto")`
  is safe because no production path stamps `run.streamingMode = "rpc"`
  yet (`plan.md:42-58`); running runs default to print-mode rejection
  branch (acceptance line `plan.md:71` pins this).
  ✅ Slice-1 tests are writable without slices 2–7.
- **Slice 2 → slice 1's types.** ✅ Citations check (`plan.md:103`).
- **Slice 3 → slice 2's `RpcStdinQueue`.** ✅ Auto-cancel handler
  synchronously enqueues (`plan.md:144-147`); slice 3 ships
  `routeRpcResponse` as a stub (`plan.md:149`).
- **Slice 4 → slice 3's stub.** ✅ Stub upgrades to real correlation
  router; `Run.pendingAcks` Map introduced here (`plan.md:184-186`).
- **Slice 5 → slice 4's `pendingAcks` + `streamingMode` stamping.**
  Partially correct (only `forceTerminate` actually consumes
  `pendingAcks`; bump-asymmetry and reconcile-orphan don't). See §8.
- **Slice 6 → slice 5's `forceTerminate`** for the live integration
  "forceTerminate mid-turn dies cleanly" case (`plan.md:283`). ✅
- **Slice 7 → slice 6's slash command + live tests** so §10 prose
  describes a working surface (`plan.md:355-356`). ✅

**Live-test gating (slice 6).** Default suite stays green when
`CONDUCTOR_LIVE_TESTS=1` is unset (1255 → ~1260; integration tests
skip-counted). Per AGENTS.md. ✅ (`plan.md:307-310`)

**Files-touched overlap.** `src/runs.ts`, `src/event-handler.ts`,
`tests/event-handler-rpc.test.ts` touched across slices. Each overlap is
additive and confined to different functions/cases (slice 3 adds RPC
dispatch arms; slice 5 extracts `bumpOnRpcLine` from those arms; slice 4
adds `pendingAcks`; slice 5 extends `forceTerminate`). No out-of-order
build risk.

---

## 3. Acceptance test list specificity

Spot-checked all 7 slices. All acceptance lines cite file + test-name +
observable assertion. Cross-references design §7 (`design.md:1084-1180`)
cleanly — every named test file has a design-§7 entry. No "tests pass" /
"behavior X works" red flags.

**Soft flag.** Slice 3's auto-cancel warning string (`plan.md:148`) is
not pinned by an explicit acceptance test (cf. W3 character-pin in
slice 1). One-line acceptance addition would match slice 1's W3
discipline; not a slip-through hazard.

---

## 4. Locked-decision compliance

| Lock / fix | Plan compliance | Citation |
|---|---|---|
| Q1 (per-spawn opt-in only; no Persona.steerable) | ✅ Slice 1 explicitly forbids stub frontmatter field | `plan.md:81-82` |
| Q2 (LLM tool surface) | ✅ Slice 4 adds `streaming_behavior` + `steerable` only | `plan.md:182-183` |
| Q6 (kill verbs unchanged; SIGTERM only) | ✅ Slice 5 SIGTERM unchanged; only `RpcStdinQueue.destroy` preamble | `plan.md:241,267` |
| Q9 (bump asymmetry: response-yes, ext-ui-no) | ✅ Slice 5 W2 both directions | `plan.md:236-237,251-253` |
| Oracle fix #1 (drop persona-frontmatter) | ✅ Pre-merge grep gate + slice 1 out-of-scope | `plan.md:81,461` |
| Oracle fix #2 (narrow bump) | ✅ Slice 5 + W2 witness | `plan.md:236-237` |
| Oracle fix #3 (orphaned RPC reclassify) | ✅ Slice 5 reuses `killed` + errorMessage prefix | `plan.md:238-240` |
| Oracle fix #4 (auto-cancel ext-ui) | ✅ Slice 3 always-cancel-and-warn | `plan.md:144-148` |
| Oracle gate 2 §4.1 ADJUST (resolver one-liner) | ✅ Slice 1 `run.steerable ?? defaultSteerable`; refactor of `resolveKillOnStall` explicitly forbidden | `plan.md:43,486` |
| §4.9 vendored RPC types (no runtime probe) | ✅ Slice 2 `src/rpc-types.ts` header-comment policy; slice 6 CONTRIBUTING callout | `plan.md:104,295,447-449` |
| PRD §"v0.10+ planned" — no sub-agent → conductor msging | ✅ Plan §8 explicitly out-of-scope | `plan.md:481` |
| PRD D1 — no new RunStatus for orphan-RPC | ✅ Slice 5 reuses `killed` + errorMessage discriminator | `plan.md:262` |

No lock violations.

---

## 5. Test-count discipline (3-slice spot check)

| Slice | Claimed | Acceptance enumerates | Gap |
|---|---|---|---|
| 1 | +26 | 5 + 8 + ~16 + 2 + 1 = **~32** | Conservative. ✅ |
| 2 | +8 | 5 + 2 = **7** | +1 unaccounted — fine. |
| 4 | +16 | 5 + 1 + 8 cascade = **14**, but the 8 cascade cases are **already in slice 1's +26 count** (`plan.md:55-57`, `plan.md:78`, `plan.md:209`). Net new = **6**. | **Off by ~10.** |
| 5 | +15 | 3 + 1 + 2 + 2 = **8** | **Off by ~7.** Either extra unenumerated cases will materialize, or trajectory is inflated. |

**Total claimed +81; spot-checked sum closer to +60–70.** This is an
ADJUST: either slice 4 / slice 5 acceptance lists need expansion to
match the trajectory, or the trajectory in `plan.md:399` and the
pre-merge gate `plan.md:456` (`≥ +81`) need relaxing to `~+65`. The
risk of holding builders to a `≥+81` floor that the enumerated cases
don't support is padded-test cruft.

---

## 6. Open-question resolutions

- **Q1 (immediate write).** Plan picks (c); design §10 Q1 defaulted to
  (a). Justified via v0.5 precedent (`PRD.md:484`) and slice-6 empirical
  pin (`plan.md:421`). **Should be flagged as a deliberate revision of
  the design default**, not buried under "Resolved." One-line ADJUST.
- **Q2 (skip get_state).** ✅ Aligns with oracle gate 2 §5 HOLD.
- **Q3 (slice 7 §10 critic gate).** ✅ Mirrors v0.11 slice-5 pattern.
  Plan only patches §10; design §10 Q3 mentioned both §10/§11. If §11 is
  structurally unchanged, slice 7 should say so explicitly (one-line
  acceptance add: "§11 unchanged; verify via existing no-regression").
- **Q4 (executor errorMessage dispatch).** ✅ Consistent with PRD D1
  (`PRD.md:524`) and v0.9.x `"orphaned: ..."` family (`PRD.md:528`).
  Executor reads literal `"orphaned: rpc-stream-detached"` — coupling is
  real but localized; refactor trigger acknowledged (`plan.md:428`).
  Right tradeoff vs new RunStatus / new executor verb. **HOLD.**

---

## 7. Risk register cross-reference

Design §6 risks 1–5 each map to a slice in plan §6 (`plan.md:432-449`):

| Design risk | Plan slice | Status |
|---|---|---|
| R1 stdin race | Slice 2 (`RpcStdinQueue` + 5 cases) | ✅ |
| R2 ext-ui blocking | Slice 3 (auto-cancel + 1 case) | ✅ |
| R3 watchdog confounding | Slice 5 (W2 both directions) | ✅ |
| R4 forceTerminate tangles | Slice 5 (queue.destroy preamble + W7) | ✅ |
| R4-adj orphan-RPC | Slice 5 (reconcile branch) | ✅ |
| R5 session-file divergence | **Out of scope** (§8 implicit; plan §6 explicit) | ✅ |

Pi-version drift risk is correctly tracked as advisory monitoring
(`plan.md:447-449`), matching oracle gate 2 §4.9 lock.

---

## 8. Slice 5 multi-fix audit

Three concerns: (a) bump asymmetry, (b) reconcile-orphan, (c)
forceTerminate stdin cleanup.

**Test-level coupling:** none. W2 tests touch `bumpOnRpcLine` only;
reconcile tests touch `classifyRecord` + executor only; forceTerminate
tests touch `RpcStdinQueue.destroy` + slice 4's `pendingAcks` only.

**Plan's "all three behind slice 4's `pendingAcks` Map" claim is
overstated.** Only (c) consumes `pendingAcks`. (a) and (b) are
independently shippable after slice 3. Could split cleanly into
5a/5b/5c without forcing out-of-order builds — slice 6 only needs 5c.

**Critic-gate burden.** Plan scopes the slice-5 gate to W2 teeth only
(`plan.md:269-270`). Reconcile-orphan correctness and W7 idempotency are
not critic-gated; ~150 LOC src + ~200 LOC test across three concerns
leaves (b) and (c) un-reviewed beyond suite-green.

**Recommendation (per task anti-goals: flag, don't re-decompose):**
slice 5 prologue should mark itself "M-bordering-XL; split-on-demand if
critic returns REVISE." Builder splits into 5a/5b/5c if mid-implementation
diff size grows unwieldy.

---

## 9. Other small drift

- **Slice 1 says cascade scaffold is "wired in slice 3"** (`plan.md:52`),
  but plan §4 table (`plan.md:391`) and slice 4 (`plan.md:200,209`) say
  wired in slice 4. **Real internal contradiction.** Pick one — slice 4
  is correct per the dependency chain (`steerable` arg is added to
  `ensemble_spawn` in slice 4, not slice 3). Fix the slice-1 sentence.
- **Config-tree placement** (`plan.md:371`): keeping `defaultSteerable`
  at top-level is a defensible planner call per oracle gate 2 §6
  ("planner discretion"). One-line mention in PRD §"Decision log" at
  slice 7 closure is appropriate but not pinned in slice 7 acceptance.
  Soft flag.
- **`PiArgsOptions` unification** (`plan.md:104`): plan says "both fresh
  + resume variants" gain `steerable`. Inspector map shows two distinct
  call sites; ensure slice 2 acceptance covers both (only fresh-variant
  argv test is enumerated; resume-variant should be too).

---

## 10. Confidence + recommendation

**Confidence: High.**

Lock-respecting and slice-ordering-correct. Three ADJUSTs are
editorial/accounting, not structural — none change the slice graph or
mutation-witness shape. Oracle gate 2 §4.1 isomorphism correction
propagated cleanly. Risk register complete; all four open questions
justified.

**Recommendation: ready for slice-1 builder after a one-paragraph
plan-§5 callout on the Q1 (c)-vs-(a) revision, a slice-1↔slice-4
cascade-wireup contradiction fix, and a trajectory-math reconciliation
(`+81` → `~+65` or expand slice-4/5 acceptance lists to match).** No
redteam pass needed at this gate. Spawn redteam after slice 6 lands if
the live RPC integration surfaces unexpected pi-coding-agent behaviour
— the genuine integration risk sits there, not in the plan structure.
