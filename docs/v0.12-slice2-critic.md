# v0.12 — Slice 2 — Critic review

**Slice:** `feat(rpc): vendor RPC types + single-writer stdin queue + RPC argv branch`
**Commit:** `21e6314`
**Parent:** `3f73d9d` (slice 1)
**Plan §:** `docs/v0.12-steering-plan.md` §"Slice 2" lines 100–137 — "Critic gates: None (mechanical — wire-format + argv flip)"
**HEAD at review:** `21e6314` (clean working tree apart from accumulated v0.12 design docs).

## Review

### Trajectory remeasure (carry-over Note from slice 1)

- ✓ **Correct: baseline reconciled.** Builder's commit body cites *"Pre-slice-2 baseline: 1212 total, 1198 pass, 14 skipped"*. I checked out `3f73d9d` and re-ran `env -u CONDUCTOR_SUBAGENT npm test`: **1212 / 1198 pass / 14 skipped**. Matches byte-for-byte. Post-slice-2 at `21e6314`: **1221 / 1207 / 14**. Active delta `+9` vs plan target `+7` — the +2 over-coverage is the two declared resume-mode mirror argv pins; over-coverage is approve-able per the brief's anti-goals.
- ✓ **Correct: slice-1 critic Note resolved.** Builder used the corrected `3f73d9d` baseline, not the phantom slice-1 figure.

### Vendored-types byte-equivalence audit (NOVEL — builder did not run a strict diff)

- ✓ **Correct: pin matches `package.json`.** `src/rpc-types.ts:4` pins `@earendil-works/pi-coding-agent@0.74.0`; `package.json` declares `"@earendil-works/pi-coding-agent": "^0.74.0"`. No drift.
- ✓ **Correct: byte-equivalence of all 6 vendored types confirmed.** Diffed each type body (whitespace + leading-pipe normalised) against `[pi-dist] modes/rpc/rpc-types.d.ts`:
  - `RpcCommand` (vendor `src/rpc-types.ts:50`–`193` vs upstream `13`–`121`) — semantically identical.
  - `RpcResponse` (vendor `231`–`444` vs upstream `147`–`345`) — only declared `unknown` erasures (`CompactionResult`, `BashResult`, `SessionStats`); 3 cosmetic trailing-semi-inside-object diffs (TS-equivalent).
  - `RpcSessionState` (vendor `208`–`228` vs upstream `133`–`145`) — **perfect match**.
  - `RpcExtensionUIRequest` (vendor `445`–`516` vs upstream `347`–`403`) — semantically identical.
  - `RpcExtensionUIResponse` (vendor `518`–`533` vs upstream `405`–`415`) — semantically identical.
  - `RpcSlashCommand` (vendor `195`–`206` vs upstream `123`–`131`) — only the declared `SourceInfo` → `unknown` erasure.
- ✓ **Correct: no undocumented drift.** All 4 erasures (`BashResult`, `CompactionResult`, `SessionStats`, `SourceInfo`) match the header's `Private dependent types … erased to unknown` claim.

### No runtime shape probe (oracle gate 2 §4.9 lock)

- ✓ **Correct: `src/rpc-types.ts` is types-only.** `grep -nE "^(const|let|var|function|class|enum)\s|^\s*(const|let|var|function|class|enum)\s" src/rpc-types.ts` returns zero matches.
- ✓ **Correct: `src/rpc-stdin.ts` constructs no specific RPC commands.** `grep -nE 'type:\s*"get_state"|type:\s*"prompt"|type:\s*"steer"' src/rpc-stdin.ts` returns zero matches — transport-only, semantic-naive.
- ✓ **Correct: `get_state` / `isStreaming` only appear inside `src/rpc-types.ts` as type declarations** (`src/rpc-types.ts:81, 213, 266`). No probe path leaked into `runs.ts`.

### Argv regression pin (byte-for-byte) — MUTATION-TESTED

- ✓ **Correct: assertion is `assert.deepEqual` on the full array** (`tests/buildPiArgs.test.ts:64`–`83`, `:104`–`112`). Not regex / partial.
- ✓ **Correct: pin has teeth.** Mutation: inserted `--debug` into the print-mode argv at `src/runs.ts:368`. Both regression pins reded:
  - `tests/buildPiArgs.test.ts:64` (`fresh, steerable=false`) — actual array gained `'--debug'` between `'-p'` and `'--session-dir'`.
  - `tests/buildPiArgs.test.ts:104` (`resume, steerable=false`) — same shape.
  Restored via `git checkout`. All 4 `tests/buildPiArgs.test.ts` cases green at HEAD.

### Single-writer queue contract — MUTATION-TESTED (NOVEL)

- ✓ **Correct: back-pressure is callback-driven.** Builder used the Node `Writable.write(chunk, enc, cb)` 3-arg callback signature (`src/rpc-stdin.ts:217`) instead of the brief's "drain event" wording. The callback contract is the stricter primitive for single-writer FIFO ordering (the next write is gated on the previous write's callback firing). Design §4.2 lines 410–447 say *"back-pressure is awaited"* without specifying mechanism — the callback signature is the canonical Node primitive for this guarantee. Acceptable deviation.
- ✓ **Correct: single-writer guard has teeth.** Mutation: removed the `this.inFlightEntry !== null` clause from `pump()` at `src/rpc-stdin.ts:179` so the queue becomes re-entrant. Two tests reded:
  - `tests/rpc-stdin.test.ts:120` (`enqueue serialises writes in submit order under back-pressure`) — `chunks.length` jumped from 1 to 3 immediately, breaking the FIFO ordering invariant.
  - `tests/rpc-stdin.test.ts:202` (`destroy(reason) rejects all pending writes`) — destroy found in-flight + queued entries already in flight; rejection reason mismatch.
  Restored.
- ✓ **Correct: recursive newline scan has teeth.** Mutation: changed `findRawLineSeparator` at `src/rpc-stdin.ts:33` to scan top-level only (return `null` for any non-string value). The `tests/rpc-stdin.test.ts:235` newline-rejection test reded specifically on the nested `images:[{content:"data\nbreak"}]` case (lines 270–283) — the recursive scan is necessary, not redundant. Restored.
- ⚠ **Note: FakeWritable duck-type bypasses real Node Writable semantics.** Builder declared this. Slice 3's live integration test under `CONDUCTOR_LIVE_TESTS=1` (per plan §"Slice 6" — actually slice 6 wires the real RPC integration) owns the real-pipe contract. Not blocking per brief's anti-goals.

### Slice-3 territory leak check

- ✓ **Correct: zero leak.**
  - `git diff 3f73d9d..21e6314 -- src/event-handler.ts` is empty.
  - `git diff 3f73d9d..21e6314 -- src/runs.ts | grep -E "runPiSubprocess|stdin\.write|stdin\.pipe|stdio:"` returns zero added lines.
  - `grep -rn "auto-cancel\|extension_ui_response" src/` only matches `src/rpc-types.ts:520, 525, 530` (vendored type declarations). No handler code.
  - `grep -rn "extension_ui_request\|RpcExtensionUIRequest" src/` excluding `rpc-types.ts` returns zero matches.

### Deliberate dead-value sentinels

- ✓ **Correct: exactly 3 production call sites.** `grep -n "steerable: false" src/runs.ts` produces 3 hits at `src/runs.ts:433` (`buildResumePiArgs`), `:605` (`planSpawnPiArgs` seeded-resume branch), `:623` (`planSpawnPiArgs` fresh branch). Each has a slice-4 ownership comment cross-referencing the cascade collapse.
- ✓ **Correct: no test-fixture dead-value writes.** Test-side `steerable: false` literals in `tests/runs-helpers.test.ts` are mechanical TS-fix from the new required field, not production sentinels.
- ✓ **Correct: slice 4 explicitly claims them.** `docs/v0.12-steering-plan.md:53` ("slice 4 has a green target … cascade collapse"), `:201` (`per-call steerable test`), `:225` (`run.steerable from the merged cascade`).

### Conventional Commits compliance (NOVEL — chose option (a) from §8)

- ✓ **Correct: subject matches plan slice-2 title verbatim.** Plan: `feat(rpc): vendor RPC types + single-writer stdin queue + RPC argv branch`. Commit: identical.
- ✓ **Correct: body cites design and trajectory.** References §4.9, design §4.2, `PRD.md:616` (Q5-deferred), pi version `@0.74.0`, the corrected `3f73d9d` baseline, and the +9 vs plan +7 deviation with rationale.

### Test suite + tsc

- ✓ **Correct: 1221 / 1207 pass / 14 skipped / 0 failing.** Builder's claim verified.
- ✓ **Correct: `npx tsc --noEmit` clean.**

## Verdict: APPROVE

Slice 2 is mechanical wire-format + argv flip as the plan required. All 6 vendored types are byte-equivalent to upstream modulo the 4 declared `unknown` erasures. The argv regression pin and both queue-invariant pins were mutation-tested and have teeth. No slice-3 territory was touched. The 3 dead-value `steerable: false` sentinels are correctly placed and slice 4 owns their replacement.

The +2 over-coverage (resume-mode mirror argv pins) is sound — closes the seam slice 4 will exercise when `sendToRun` produces RPC-shaped resume argv. The pre-stringify recursive newline scan deviation from the plan's "post-stringify dead-by-construction" wording is a strengthening, not a regression: the recursive variant catches caller-visible bugs in nested string fields that JSON-escape would otherwise hide.

Slice 3 is unblocked.
