# Plan: v0.8.3 Item 3 — foreground transcript view + Ctrl+G overlay

Status: **plan** (no code). Source: `docs/v0.8.3-item3-design.md` (APPROVED with amendments) +
`docs/v0.8.3-item3-oracle-review.md`. HEAD at plan time: `f38d557`, tree clean.

---

## Preamble — investigations + amendments resolved up-front

### A1. Box dropped from D4 #3 (oracle amendment 1)

Slice 10 (overlay empty-state polish) MUST NOT use `Box` from pi-tui. Treatment is
`theme.fg("dim", …)` over the prose + `theme.fg("muted", …)` on a 1-char heading +
1 spacer line for vertical centering + drop the flanking rulers. Aligns with §8 deferral
of `Box`/`DynamicBorder`-based work to v0.9.

### A2. Test-count footnote (oracle amendment 2)

Design.md line ~88 cites "50+ existing tests" justifying the Option-A pure-renderer pick.
Actual counts (verified): **19 tests in `tests/transcript.test.ts`, 15 in
`tests/foreground-stream.test.ts`** (34 total across the two snapshot-heavy files).
Decision stands — Option A still wins on TDD cost, code locality, and dep surface.
Just a stale magnitude claim; not a re-decision.

### A3. Slice 2 `is_error` verification (oracle amendment 3) — RESOLVED

Verification done at plan time:

```bash
grep -n "isError" node_modules/@earendil-works/pi-ai/dist/types.d.ts
# 158: export interface ToolResultMessage<TDetails = any> {
# 164:     isError: boolean;
# 167: export type Message = UserMessage | AssistantMessage | ToolResultMessage;
```

`isError: boolean` is a **required** field on `ToolResultMessage` from `@earendil-works/pi-ai`,
re-exported through `pi-agent-core`. **Slice 2 does NOT need to touch `src/types.ts`.**
The field is read directly off the result message. Slice 2 file list updated below
(types.ts removed).

### A4. 5a/5b split decision — APPLIED

Verified at plan time:

- `src/types.ts:178` — `Run` has `lastToolCall?: string` only.
- `src/runs.ts:867,901` — `lastToolCall` is the only "last X" field; no `lastEventAt` /
  `lastUpdateAt` / `lastMessageAt`.
- `src/event-handler.ts:62,94` — events push to `run.messages` but no timestamp is
  written to `Run` on push.

→ **Slice 5 is split into 5a (infra: add `lastEventAt`) + 5b (UX: consume in header)**
per oracle's recommendation. Final slice count: **14** (was 13).

### A5. Foreground thinking-summary line (oracle watch-for, slice 3)

Decision pinned: **foreground stream renders the thinking-summary line unconditionally.**
There is no overlay model accessible to `renderForegroundStream` (it has no input loop,
is one-shot per render). The "model toggle is source of truth" is overlay-only. Foreground
gets summary always; users who want the body open the overlay (Ctrl+G) and press `t`.

### A6. Snapshot-thrash budget

Pre-allocated. See §6 below. Estimate: **8–12 of 34 existing tests** in `transcript.test.ts`
+ `foreground-stream.test.ts` will need updates across slices 1–5. Plan distributes the
thrash so no single slice is forced into >5 simultaneous snapshot rewrites.

### A7. PRD Esc → detach lock — load-bearing in slice 9

`PRD.md:22, 255–256, 531` lock Esc → detach in foreground spawns. Slice 9 (overlay
`FOOTER_BINDINGS` consolidation) MUST preserve the existing overlay-Esc → `onClose`
routing. Acceptance criterion pinned in slice 9 spec.

### A8. Factory listener-leak invariant — load-bearing in slice 11

`src/focused-overlay-factory.ts` has a documented invariant: factory MUST NOT register a
`RunRegistry` listener (one no-op listener leaked per overlay open in a previous
implementation). Slice 11 moves `model.refresh()` out of `render()` into
`installFocusedOverlayShortcut` (session-scoped, idempotent) — must keep the factory
clean. Acceptance pinned in slice 11.

---

## Slice ordering

### Load-bearing (do NOT reorder)

- **0 first.** S0 introduces `src/status-glyph.ts` and `src/tool-summary.ts`; S1–S5
  import from those files. Reordering forces a mid-tree merge.
- **6 before 7.** `src/transcript-classify.ts` is consumed by `src/transcript-style.ts`.
- **7 before 8/9/10/11.** All overlay-polish slices depend on the styling layer.
- **5a before 5b.** 5a adds `Run.lastEventAt` field; 5b reads it in `renderHeader`.
- **12 last.** Backlog-close cites all SHAs; must run after every behavioral commit.

### Recommended serial execution order (linear)

```
0 → 1 → 2 → 3 → 4 → 5a → 5b → 6 → 7 → 8 → 9 → 10 → 11 → 12
```

Linear order is recommended for the builder ⇄ critic loop — simpler to gate, simpler
snapshot conflict resolution, easier verifier story.

### Parallelizable (future runs only — not for this chain)

- S1, S2, S3, S4, S5a+5b can land in any order (each owns its test file).
- S8, S9, S10, S11 can land in any order after S7 (they touch
  `focused-stream-overlay.ts` in different methods/regions; expect trivial
  imports-block merge).

---

## Per-slice specs

### Slice 0 — Pre-redesign dedup: shared `STATUS_GLYPH` + tool-summary core

**Size:** S
**Depends on:** —
**Files touched:** `src/status-glyph.ts` (new), `src/tool-summary.ts` (new),
`src/transcript.ts`, `src/foreground-stream.ts`, `src/widget.ts`, `src/event-handler.ts`,
`tests/status-glyph.test.ts` (new), `tests/tool-summary.test.ts` (new)
**New tests:** `tests/status-glyph.test.ts`, `tests/tool-summary.test.ts`

**Sub-issue(s) addressed:** v0.7 critic finding — duplicate `STATUS_GLYPH` (3× in
src/, plus `statusGlyph()` switch in `widget.ts`) and duplicate
`summarizeArgs` vs `formatToolCallShort`.

**Pre-implementation verification:**
- `grep -n "STATUS_GLYPH\|statusGlyph" src/transcript.ts src/foreground-stream.ts src/widget.ts`
  confirms 3 plain glyph maps + 1 switch in widget.

**Failing test (Red):**
- Test name: `status-glyph: STATUS_GLYPH exposes a plain (unstyled) char per RunStatus`
- Asserts: `STATUS_GLYPH.running === "●"`, `.completed === "✓"`, `.failed === "✗"`,
  `.killed === "■"`, `.timeout === "⏱"`, `.queued === "◌"`, `.paused === "⏸"`.
- Test name: `tool-summary: summarizeToolArgs returns base summary for bash/read/write/edit/grep`
- Asserts: `summarizeToolArgs("bash", { command: "echo hi" }) === "echo hi"`;
  `summarizeToolArgs("read", { path: "x.ts" })` returns the path summary;
  default branch produces compact `key=value` pairs.

**Implementation (Green):**
- Create `src/status-glyph.ts` exporting `STATUS_GLYPH: Record<RunStatus, string>` with
  the 7 plain chars (no theme).
- Create `src/tool-summary.ts` exporting `summarizeToolArgs(name, args)` — the *core*
  helper without `$ ` prefix or `shortenPath` opinions.
- Replace local `STATUS_GLYPH` in `transcript.ts`, `foreground-stream.ts` with import.
- Replace `widget.ts` switch arm chars with `STATUS_GLYPH[s]` (keep `theme.fg(...)` wrap).
- Replace `transcript.ts:summarizeArgs` body with delegation to `summarizeToolArgs`
  (the existing `summarizeArgs` is the bare core; same shape).
- `event-handler.ts:formatToolCallShort` becomes a thin wrapper:
  `\`$ ${shortenPath(...) wrapper applied per-tool}\`` — re-uses
  `summarizeToolArgs` *only* where it doesn't conflict with the `$ ` prefix /
  `shortenPath` opinions; the `$ ` and `shortenPath` calls remain in
  `event-handler.ts`.

**Acceptance criteria:**
- [ ] All listed assertions pass.
- [ ] `npm test` green.
- [ ] `npx tsc --noEmit` clean.
- [ ] Pre-commit hook auto-rebuilds `dist/index.js` + sourcemap.
- [ ] **`event-handler.ts:84` (`run.lastToolCall = formatToolCallShort(...)`) emits the
  same string shape as before — `run.lastToolCall` text in widget output is byte-identical
  for at least bash/read/write/edit/grep cases (regression test).**
- [ ] No widget snapshot/test regressions — existing `tests/event-handler.test.ts`
  passes unchanged.
- [ ] No new top-level dependencies.

**Risks:** Bounded — pure re-export. Risk: easy to accidentally drop the `$ ` prefix
on `lastToolCall`. Mitigation: dedicated regression assertion.

---

### Slice 1 — `shortenMiddle` for bash arg tails

**Size:** S
**Depends on:** 0
**Files touched:** `src/tool-summary.ts`, `tests/tool-summary.test.ts`
**New tests:** addition to `tests/tool-summary.test.ts`

**Sub-issue(s) addressed:** v0.8.1 #1 — *"Truncation cuts at the wrong place — `bash aws sts get-caller-identity --profile tickety 2>/…` and `bash ada credentials update --account=$DEV_ACCOUNT_ID …` hide the meaningful argument tail."*

**Pre-implementation verification:**
- `grep -n "shorten\b" src/transcript.ts src/event-handler.ts src/tool-summary.ts`
  confirms current `shorten` is head+ellipsis only.

**Failing test (Red):**
- Test name: `tool-summary: bash uses shortenMiddle (head+ellipsis+tail)`
- Asserts: `summarizeToolArgs("bash", { command: "aws sts get-caller-identity --profile tickety 2>/dev/null" })`
  contains both `"aws sts"` (head) and either `"tickety"` or `"/dev/null"` (tail) and contains `"…"`.
- Test name: `tool-summary: short bash commands stay verbatim`
- Asserts: `summarizeToolArgs("bash", { command: "echo hi" }) === "echo hi"`.
- Test name: `tool-summary: shortenMiddle preserves total budget`
- Asserts: result `.length <= 50` for the long input.

**Implementation (Green):**
- Add `shortenMiddle(s, max)` helper inside `tool-summary.ts`. Allocates ~60% to head, ~40%
  to tail, with `"…"` joiner. Falls back to `shorten` for very short budgets (`max < 16`).
- `summarizeToolArgs` for `bash` calls `shortenMiddle(cmd, 50)` instead of `shorten(cmd, 50)`.
- Other tool branches (`read`/`write`/`edit`/`grep`) keep `shorten` (leading info matters
  most for paths and patterns).

**Acceptance criteria:**
- [ ] All listed assertions pass.
- [ ] `npm test` green; `npx tsc --noEmit` clean.
- [ ] Existing `tests/transcript.test.ts:112` ("collapsed toolCall renders as a single
  chevron line") still passes (echo case unchanged).
- [ ] Snapshot/text-shape change is contained to `tool-summary.ts`.

**Risks:** Snapshot thrash to ~1 assertion in `transcript.test.ts:112` if its bash-arg
literal triggers shortenMiddle (uses `"echo hi"`, won't); confirm during Red phase.

---

### Slice 2 — Tool-call outcome line `↳ ✓/✗ <preview>` in collapsed mode

**Size:** M (touches both render branches + adds branching on `isError`)
**Depends on:** 0
**Files touched:** `src/transcript.ts`, `tests/transcript.test.ts`
**New tests:** added cases in `tests/transcript.test.ts`

**Sub-issue(s) addressed:** v0.8.1 #2 — *"Tool results are invisible — every `▶ bash X` lacks a follow-up `✓/✗` outcome line. User can't tell at a glance whether the command succeeded."*

**Pre-implementation verification:**
- ✅ A3 already done: `isError: boolean` is on `ToolResultMessage` from
  `@earendil-works/pi-ai`. **`src/types.ts` is NOT in the file list.**

**Failing test (Red):**
- Test name: `renderTranscript: collapsed toolCall + success result emits a ↳ ✓ outcome line`
- Asserts: lines contain `"▸ bash"` AND a sibling line matching `/^\s*↳ ✓ /` with the
  result preview.
- Test name: `renderTranscript: collapsed toolCall + isError result emits ↳ ✗`
- Asserts: lines contain `/^\s*↳ ✗ /` and the error preview.
- Test name: `renderTranscript: collapsed toolCall without a matched result emits no ↳`
- Asserts: lines contain `▸` but no `↳` glyph.
- Test name: `renderTranscript: outcome line preview is firstLine + truncated to width`
- Asserts: outcome line `.length <= width`.

**Implementation (Green):**
- In `renderToolCall`, after the collapsed `▸ ${name}` line emit:
  ```ts
  const result = part.id ? resultsByCallId.get(part.id) : undefined;
  if (result) {
    const text = extractFirstResultText(result);
    const glyph = result.isError ? "✗" : "✓";
    out.push(truncateOrPad(`  ↳ ${glyph} ${firstLine(text)}`, opts.width));
  }
  ```
- Helper `extractFirstResultText` reads `result.content[0]?.text ?? .output ?? ""`.
- Expanded mode (`!collapseToolCalls`) keeps existing `↳ <line>` behavior; the
  `↳ ✓/✗` outcome line is collapsed-mode-only (per design — expanded already shows
  full output).

**Acceptance criteria:**
- [ ] All listed assertions pass.
- [ ] `npm test` green; `npx tsc --noEmit` clean.
- [ ] Existing `tests/transcript.test.ts:186` ("toolResult message renders indented under
  the call") still passes (it uses `collapseToolCalls: false`).
- [ ] Existing `tests/transcript.test.ts:112` may need a helper-fixture adjustment to
  add an `isError: false` matched result for the new branch path — pre-allocate +1
  snapshot rewrite.
- [ ] **Throttle interaction note (oracle):** If `createUpdateThrottle` causes a tool
  call to render before its result, no outcome line — that's correct behavior; the
  trailing-edge re-render covers it.
- [ ] Foreground stream picks up the change for free (uses same `renderTranscript`).

**Risks:** Snapshot thrash in 1 existing test. Throttle leading-edge can show
"call without outcome" for ≤500ms — accepted (oracle §7).

---

### Slice 3 — Thinking summary line (default visible)

**Size:** S
**Depends on:** 0
**Files touched:** `src/transcript.ts`, `src/foreground-stream.ts`,
`tests/transcript.test.ts`, `tests/foreground-stream.test.ts`
**New tests:** added cases in both files

**Sub-issue(s) addressed:** v0.8.1 #3 — *"Thinking blocks hidden by default with no inline toggle hint (the `t` keybind exists only in the focused-stream overlay footer)."*

**Pre-implementation verification:**
- `grep -n "showThinking" src/transcript.ts src/foreground-stream.ts` confirms current:
  `transcript.ts` already supports `showThinking` flag; `foreground-stream.ts` hardcodes
  `showThinking: false`.

**Failing test (Red):**
- Test name: `renderTranscript: thinking with showThinking=false renders a one-line summary`
- Asserts: lines contain a row matching `/· thinking \(\d+ chars/` and DO NOT contain
  the body text.
- Test name: `renderTranscript: thinking with showThinking=true renders body (no summary line)`
- Asserts: body text present, summary `· thinking (` line absent.
- Test name: `renderForegroundStream: thinking renders a one-line summary unconditionally`
- Asserts: foreground always shows the `· thinking (...)` summary; never the body
  (pinned per A5).

**Implementation (Green):**
- In `transcript.ts` thinking path: when `!opts.showThinking`, emit a single line
  `  · thinking (${chars} chars / ${lines} lines)` instead of the current "skip" behavior.
- Foreground stream: `renderForegroundStream` keeps `showThinking: false` so it gets
  the summary unconditionally (per A5; no flag added).
- Existing `tests/transcript.test.ts:150` ("thinking blocks are hidden by default")
  must be re-asserted: now a *summary line* is present even with the default. Replace
  the old "no thinking text" assertion with "no body, but summary line present."

**Acceptance criteria:**
- [ ] All listed assertions pass.
- [ ] `npm test` green; `npx tsc --noEmit` clean.
- [ ] **Foreground stream renders summary unconditionally** (oracle watch-for; A5).
- [ ] `tests/transcript.test.ts:150` rewritten — pre-allocated snapshot rewrite.
- [ ] `tests/foreground-stream.test.ts:120` ("thinking blocks are hidden") rewritten
  similarly — pre-allocated.
- [ ] Footer hint already includes `t thinking` (existing) — unchanged.

**Risks:** Snapshot rewrites in 2 existing tests. Both pre-allocated.

---

### Slice 4 — One-line dim `· turn N` separator + drop trailing rule

**Size:** S
**Depends on:** 0
**Files touched:** `src/transcript.ts`, `tests/transcript.test.ts`
**New tests:** added cases in `tests/transcript.test.ts`

**Sub-issue(s) addressed:** v0.8.1 #4 + #6 — *"Flat visual hierarchy — every turn gets the same horizontal-rule + `— turn N` divider; nothing scans"* and *"Vertical waste — turn dividers + rules + whitespace stack to ~5 lines of chrome per visible turn."*

**Pre-implementation verification:**
- `grep -n "turnSeparator\|── turn " src/transcript.ts` confirms current 3-line
  ruler+label+ruler chrome.

**Failing test (Red):**
- Test name: `renderTranscript: turn separator is a single dim line, not a ruler block`
- Asserts: between adjacent assistant turns, exactly one separator line is emitted matching
  `/^\s*· turn \d+\s*$/` (NOT `─{3,}`).
- Test name: `renderTranscript: no trailing horizontal rule after the last turn`
- Asserts: last 3 lines do not all match `─{3,}`.

**Implementation (Green):**
- Replace `turnSeparator` body with `  · turn ${n}` (no rulers).
- Drop the bottom-of-turn ruler emission entirely; turn separator is the only
  inter-turn chrome.
- Note: the **header** ruler stays (top-of-header only — bottom is dropped in slice 7).

**Acceptance criteria:**
- [ ] All listed assertions pass.
- [ ] `npm test` green; `npx tsc --noEmit` clean.
- [ ] `tests/transcript.test.ts:227` ("multiple assistant turns are separated by a turn
  header") needs its regex updated from `/turn\s+\d+/i` to match the new format —
  pre-allocated snapshot rewrite.
- [ ] Visible vertical chrome per turn drops by ≥2 lines.

**Risks:** 1 existing assertion regex update.

---

### Slice 5a — `Run.lastEventAt` field (infra, no UX)

**Size:** S
**Depends on:** 0
**Files touched:** `src/types.ts`, `src/runs.ts`, `src/event-handler.ts`,
`tests/event-handler.test.ts` (or new `tests/runs-last-event-at.test.ts`)
**New tests:** assertion file (small)

**Sub-issue(s) addressed:** Infra prerequisite for v0.8.1 #5 (live activity).

**Pre-implementation verification:**
- ✅ A4 already done: `Run` has no `lastEventAt`/`lastUpdateAt`/`lastMessageAt`. Field is needed.

**Failing test (Red):**
- Test name: `event-handler: pushing a message updates run.lastEventAt to ~now`
- Asserts: after handling a `messageEvent`, `run.lastEventAt` is within 50ms of `Date.now()`.
- Test name: `event-handler: each subsequent event monotonically advances lastEventAt`
- Asserts: two pushes ≥10ms apart produce strictly increasing `lastEventAt`.

**Implementation (Green):**
- Add `lastEventAt?: number` to the `Run` interface (`src/types.ts:178`-area).
- In `src/event-handler.ts`, on every event that mutates `run.messages` (lines 62, 94),
  set `run.lastEventAt = Date.now()`.
- Initialize `lastEventAt` to `startedAt` in `runs.ts` where the run is constructed
  (so it's always defined for active runs).

**Acceptance criteria:**
- [ ] All listed assertions pass.
- [ ] `npm test` green; `npx tsc --noEmit` clean.
- [ ] **No UX change.** Verifier MUST confirm `renderTranscript` / `renderHeader` /
  `renderForegroundStream` output is byte-identical to pre-slice (golden test fixture).
- [ ] Existing `tests/event-handler.test.ts` passes; only new assertions added.
- [ ] Pre-commit hook rebuilds `dist/`.

**Risks:** Field on optional `Run` shape may surface in serialization (`record.json`).
Verify `runs.ts` JSON write/read round-trips cleanly. Mitigation: include in the
new test file.

---

### Slice 5b — Header `<activity>` field consumed from 5a

**Size:** M (header layout + idle predicate + width discipline)
**Depends on:** 5a
**Files touched:** `src/transcript.ts`, `tests/transcript.test.ts`
**New tests:** added cases in `tests/transcript.test.ts`

**Sub-issue(s) addressed:** v0.8.1 #5 — *"`Working...` is a constant string — no signal about whether persona is mid-thinking, mid-bash, mid-network, or stuck."*

**Pre-implementation verification:**
- `grep -n "renderHeader\|Working" src/transcript.ts` confirms existing header layout
  + presence of any `Working` literal.

**Failing test (Red):**
- Test name: `renderHeader: running run with last message thinking shows "thinking" activity`
- Asserts: header line contains `· thinking` after the elapsed/usage block.
- Test name: `renderHeader: running run with last message a tool call shows "$ <toolname>"`
- Asserts: header line contains `· $ bash` (or whatever last tool).
- Test name: `renderHeader: running run with lastEventAt > 5s ago shows "idle Ns"`
- Asserts: header line contains `· idle 6s` (using fake clock).
- Test name: `renderHeader: terminal status (completed/failed/etc) does NOT show <activity>`
- Asserts: header line for `completed` is unchanged from pre-slice (no idle/thinking).
- Test name: `renderHeader: width discipline — activity is truncated, not the persona name`
- Asserts: at width=50, persona name fully present, activity ellipsised.

**Implementation (Green):**
- Add `deriveActivity(run, nowMs): string | undefined` pure helper in `transcript.ts`:
  - If status not `running`/`paused` → `undefined`.
  - If `nowMs - (run.lastEventAt ?? run.startedAt) >= 5000` → `idle ${secs}s`.
  - Else inspect `run.messages.at(-1)`: thinking content → `thinking`;
    last assistant tool call → `$ ${toolName}`;
    text-only → `responding`.
- `renderHeader` interpolates ` · ${activity}` after the elapsed/usage segment, with
  width budget (truncate activity, never persona).
- Pass `now: () => Date.now()` injectable for tests (default `Date.now`).

**Acceptance criteria:**
- [ ] All listed assertions pass.
- [ ] `npm test` green; `npx tsc --noEmit` clean.
- [ ] Existing `tests/transcript.test.ts:242` ("renderHeader: shows persona, id, status,
  elapsed") needs its regex broadened to allow trailing activity — pre-allocated rewrite.
- [ ] Existing `:266` ("never exceeds the requested width") MUST still pass — width
  discipline asserts.
- [ ] Foreground stream picks it up automatically (uses `renderHeader`).

**Risks:** Width discipline is the live wire — `renderHeader` already truncates; we add
one more variable-width segment. Mitigation: pin a width=50 / width=120 / width=200
regression assertion.

---

### Slice 6 — Pure renderer split: `transcript-classify.ts` extraction

**Size:** S (refactor; no UX change)
**Depends on:** 1, 2, 3, 4, 5b (so the line shapes being classified are final)
**Files touched:** `src/transcript-classify.ts` (new), `src/transcript.ts`,
`tests/transcript-classify.test.ts` (new)
**New tests:** `tests/transcript-classify.test.ts`

**Sub-issue(s) addressed:** Architectural prerequisite for slice 7 (Option-A styling).

**Pre-implementation verification:**
- `ls src/` confirms no `transcript-classify.ts` exists.

**Failing test (Red):**
- Test name: `classify: line starting with ▸/▾ → "tool"`.
- Test name: `classify: line starting with "  ↳ ✓" or "  ↳ ✗" → "outcome"`.
- Test name: `classify: line starting with "  ┃" or "  · thinking" → "thinking"`.
- Test name: `classify: line matching "  · turn N" → "turnSep"`.
- Test name: `classify: line in header sentinel → "header"` (uses sentinel-marked input).
- Test name: `classify: anything else → "text"`.

**Implementation (Green):**
- New file `src/transcript-classify.ts` exports
  `LineKind = "header" | "turnSep" | "tool" | "outcome" | "thinking" | "text" | "footer"`
  and `classifyLine(line: string): LineKind`.
- To avoid sniffing brittleness for header lines, `renderHeader` and `renderFooter`
  prefix their output with U+2063 (invisible separator) sentinels OR — preferred
  alternative — emit a parallel `kinds: LineKind[]` array. Decision: **emit a parallel
  `kinds[]`** from the renderer functions; `classifyLine` exists for the body lines
  that need post-hoc classification (tool/outcome/thinking/turnSep/text).
- This means `transcript.ts` exports `renderHeaderKinds(width): LineKind[]`,
  `renderFooterKinds(width): LineKind[]` alongside the existing string returners. NO
  visible behavior change — kinds are not consumed yet (slice 7 wires them up).

**Acceptance criteria:**
- [ ] All listed assertions pass.
- [ ] **No visible string-output change** — verifier MUST confirm
  `renderTranscript`/`renderHeader`/`renderFooter` string output is byte-identical
  to pre-slice (re-run all existing snapshots; expect zero diffs).
- [ ] `npm test` green; `npx tsc --noEmit` clean.

**Risks:** None significant — pure addition. Confirm the parallel `kinds[]` arrays
match length of returned `string[]` arrays (regression test).

---

### Slice 7 — Component-layer styling: `transcript-style.ts` + integration

**Size:** M (touches 4 files; introduces ANSI; D3 ruler-drop folded in)
**Depends on:** 6
**Files touched:** `src/transcript-style.ts` (new), `src/focused-stream-overlay.ts`,
`src/foreground-stream.ts`, `src/transcript.ts` (drop bottom-header ruler),
`tests/transcript-style.test.ts` (new),
plus updates to `tests/focused-stream-overlay.test.ts`,
`tests/foreground-stream.test.ts`
**New tests:** `tests/transcript-style.test.ts`

**Sub-issue(s) addressed:** v0.8.1 #7 (monochrome) + #8 (header/body disconnect) +
D3 (drop bottom ruler, accent-tie) + O5 (header status hue).

**Pre-implementation verification:**
- **5-min spike (executed during slice work, not blocker):** Confirm `theme.fg` is
  stub-compatible by writing a test that monkey-patches `theme.fg` to `(slot, s) => "[" + slot + "]" + s + "[/]"`
  and renders the overlay. If `Theme` is a class with private state that resists
  patching, slice 7 expands to introduce a `applyTheme(line, kind, themeFn)` whose
  third argument is the bare `(slot, s) => string` function (no `Theme` object) so
  tests can pass a sentinel function directly. **Recommend the second approach
  preemptively** — it's smaller and decouples cleanly.

**Failing test (Red):**
- Test name: `applyTheme: header lines get "accent" slot on glyph + persona name`
- Asserts: applied output contains `[accent]`-sentinel around the glyph and persona
  segment.
- Test name: `applyTheme: tool lines get "muted" first-char accent`
- Asserts: tool line first non-space char wrapped in `[accent]…[/]`.
- Test name: `applyTheme: outcome line ✓ → "success" slot, ✗ → "error" slot`
- Asserts: green/red sentinels on the glyph chars.
- Test name: `applyTheme: header status hue tracks run.status (running→accent, completed→success, failed→error, …)`
- Asserts: 5 cases, one per status family.
- Test name: `applyTheme: thinking summary uses "dim" slot`.
- Test name: `applyTheme: turn separator uses "dim" slot`.
- Test name: `transcript: bottom-of-header ruler is no longer emitted`
- Asserts: `renderHeader` output has exactly 1 `─{3,}` line, not 2.

**Implementation (Green):**
- New `src/transcript-style.ts` exports
  `applyTheme(line: string, kind: LineKind, themeFn: (slot: string, s: string) => string, ctx?: { runStatus?: RunStatus }): string`.
- `FocusedStreamOverlay.render` post-processes its output:
  ```ts
  const lines = renderHeader(run, width);
  const kinds = renderHeaderKinds(width);
  const body = renderTranscript(run, opts);
  const bodyKinds = body.map(classifyLine);
  return [...lines, ...body, ...renderFooter(width)]
    .map((l, i) => applyTheme(l, kindsAt(i), theme.fg.bind(theme), { runStatus: run.status }));
  ```
- `renderForegroundStream` does the same wrap inline.
- `renderHeader` drops the bottom ruler emission (keeps only the top ruler) per D3.

**Acceptance criteria:**
- [ ] All listed assertions pass.
- [ ] `npm test` green; `npx tsc --noEmit` clean.
- [ ] **Pure renderer snapshots stay monochrome** — `tests/transcript.test.ts` does NOT
  contain ANSI bytes anywhere.
- [ ] Existing `tests/transcript.test.ts:242` (renderHeader regex) needs trailing-ruler
  reference updated — pre-allocated.
- [ ] Existing `tests/transcript.test.ts:266` (width never exceeded) MUST still pass —
  styling is post-render and must not change visible width.
- [ ] **Spike result documented** in commit body: "theme.fg stub-compatible: yes/no;
  test path used: <…>".

**Risks:** Width invariant — applying ANSI escapes increases byte-length. The width
assertion measures `String.length` (raw codepoint count). Sentinel-style stubs in tests
preserve `.length` only if we strip them; alternatively, tests pass a real `theme.fg`
no-op `(slot, s) => s` and assert on the unstyled bytes. Mitigation: tests use no-op
themeFn for width assertions; sentinel themeFn only for color-presence assertions.

---

### Slice 8 — Overlay scroll-hint line

**Size:** S
**Depends on:** 7
**Files touched:** `src/focused-stream-overlay.ts`,
`tests/focused-stream-scroll-hint.test.ts` (new)
**New tests:** `tests/focused-stream-scroll-hint.test.ts`

**Sub-issue(s) addressed:** O2 — *"No scroll-position indicator — `model.scrollOffset()` slices silently."*

**Pre-implementation verification:**
- `grep -n "scrollOffset\|slice\|scrollUp\|scrollDown" src/focused-stream-overlay.ts src/focused-stream-model.ts`
  confirms the slice happens silently today.

**Failing test (Red):**
- Test name: `scroll-hint: offset=0 and transcript fits → no hint emitted`.
- Test name: `scroll-hint: offset>0 and more below → both arrows: ↑ N hidden  ·  ↓ M hidden`.
- Test name: `scroll-hint: at tail (offset = lines - viewport) → only up arrow`.
- Test name: `scroll-hint: at head + transcript exceeds viewport → only down arrow`.

**Implementation (Green):**
- New helper `renderScrollHint(scrollOffset, transcriptLines, viewportHeight): string | null`.
- Suppress when `scrollOffset === 0 && viewportHeight >= transcriptLines`.
- Output: `↑ ${N} hidden  ·  ↓ ${M} hidden` (one OR both arrows).
- Rendered between transcript body and footer; styled `dim` via `applyTheme` from S7
  (`kind: "footer"` or new `kind: "scrollHint"`).

**Acceptance criteria:**
- [ ] All listed assertions pass.
- [ ] `npm test` green; `npx tsc --noEmit` clean.
- [ ] Hint line never exceeds width.
- [ ] `dim` slot applied via S7 styling layer.

**Risks:** None significant.

---

### Slice 9 — Overlay `FOOTER_BINDINGS` consolidation + `keyHint` styling

**Size:** S (consolidates O3 + O7)
**Depends on:** 7
**Files touched:** `src/focused-stream-overlay.ts`, `src/transcript.ts` (delete
`FOOTER_HINTS` const), `tests/focused-stream-overlay.test.ts`,
`tests/footer-bindings.test.ts` (new)
**New tests:** `tests/footer-bindings.test.ts`

**Sub-issue(s) addressed:** O3 (footer monochrome/hard-truncated) + O7 (`FOOTER_HINTS`
decoupled from key dispatch).

**Pre-implementation verification:**
- `grep -n "FOOTER_HINTS\|handleInput" src/transcript.ts src/focused-stream-overlay.ts`
  confirms current decoupling.

**Failing test (Red):**
- Test name: `footer-bindings: single source of truth — hint list and dispatch use the same array`
- Asserts: every binding's `key` appears in the rendered footer and dispatches via
  `handleInput`.
- Test name: `footer-bindings: adding a new binding shows up in both hints and dispatch`
  (regression-style — uses a stub array).
- Test name: **`footer-bindings: Esc → onClose still wired after refactor` (PRD lock; oracle watch-for)**
- Asserts: `handleInput("\x1b")` calls `onClose` exactly once.
- Test name: `footer: keyHint applies "keyText"-style accent on key char`
- Asserts: rendered footer (with sentinel themeFn) wraps the literal key char in
  `[accent]…[/]` or equivalent slot.

**Implementation (Green):**
- New `FOOTER_BINDINGS: Array<{ key: string; label: string; action: (overlay) => void }>`
  in `focused-stream-overlay.ts`.
- `renderFooter`-equivalent (overlay's footer renderer) derives hint strings from
  `FOOTER_BINDINGS`.
- `handleInput` dispatches via the same array.
- Delete `FOOTER_HINTS` const from `transcript.ts`. (Pure renderer no longer owns
  the hint list — only the overlay does, since the foreground card never had
  interactive bindings.)
- `transcript.ts:renderFooter` for foreground-stream/no-input contexts emits a
  static "Esc detach" hint only.

**Acceptance criteria:**
- [ ] All listed assertions pass.
- [ ] `npm test` green; `npx tsc --noEmit` clean.
- [ ] **Esc → `onClose` survives `FOOTER_BINDINGS` refactor (test pinned).** [PRD lock]
- [ ] Existing `tests/transcript.test.ts:279,286,291,297` (renderFooter Esc/Tab/c/t/s
  hints) — most of these will now live in `tests/focused-stream-overlay.test.ts`
  (where the hints actually originate). **Pre-allocated test relocation** (≤4 tests
  moved, not rewritten). `transcript.ts:renderFooter` retains a stub Esc-only test.
- [ ] No regression on Tab cycle, `c` collapse, `t` thinking, `s` send keystrokes.

**Risks:** Test relocation rather than thrash; pre-allocated. Esc routing is the
critical lock — pin twice (overlay test + integration test).

---

### Slice 10 — Overlay empty-state polish

**Size:** S (Box dropped per A1)
**Depends on:** 7
**Files touched:** `src/focused-stream-overlay.ts`, `tests/focused-stream-overlay.test.ts`
**New tests:** added cases

**Sub-issue(s) addressed:** O4 — *"Empty state is two ruler lines around 5 lines of unstyled placeholder prose — looks like a render bug."*

**Pre-implementation verification:**
- `grep -n "renderEmpty\|no sub-agents\|empty" src/focused-stream-overlay.ts` confirms
  current empty-state body.

**Failing test (Red):**
- Test name: `overlay empty state: no flanking rulers, dim prose, muted heading`
- Asserts: rendered output for empty registry contains NO `─{3,}` lines; prose lines
  classify as `dim` slot; heading line classifies as `muted`.
- Test name: `overlay empty state: vertical centering — at viewport=20, body lands ~mid`
- Asserts: body's first line index is between 5 and 12 (loose range; not a layout test).

**Implementation (Green):**
- `renderEmpty()` emits:
  - 1× spacer (`""`).
  - 1× heading line (e.g. `(no sub-agents running)`) classified as `muted`.
  - 1× spacer.
  - 1–2 prose lines classified as `dim`.
  - Trailing spacers to reach approximate vertical center for the requested viewport.
- **NO `Box`** (per A1).
- **NO flanking rulers.**

**Acceptance criteria:**
- [ ] All listed assertions pass.
- [ ] `npm test` green; `npx tsc --noEmit` clean.
- [ ] Slot classifications wired through S7 styling layer.

**Risks:** Vertical-centering math; loose-range assertion avoids brittleness.

---

### Slice 11 — Render purity: move `model.refresh()` out of `render()`

**Size:** M (touches 3 files; lifecycle wiring)
**Depends on:** 7
**Files touched:** `src/focused-stream-overlay.ts`, `src/focused-overlay-shortcut.ts`,
`src/focused-overlay-factory.ts`, `tests/focused-stream-overlay.test.ts`,
`tests/focused-overlay-shortcut.test.ts`, `tests/focused-overlay-factory.test.ts`,
plus a new `tests/overlay-listener-leak.test.ts`
**New tests:** `tests/overlay-listener-leak.test.ts`

**Sub-issue(s) addressed:** O6 — *"`render()` mutates `model.refresh()` as a side effect."*

**Pre-implementation verification:**
- `grep -n "model.refresh()\|onChange\|registry.on" src/focused-stream-overlay.ts src/focused-overlay-shortcut.ts src/focused-overlay-factory.ts`
  confirms current side-effect.

**Failing test (Red):**
- Test name: `overlay render is pure: render(width) does NOT call model.refresh`
- Asserts: spy on `model.refresh` shows zero calls during `render`.
- Test name: `installFocusedOverlayShortcut subscribes to registry on first install`
- Asserts: `registry.on(...)` called exactly once.
- Test name: **`installFocusedOverlayShortcut: re-opening the overlay does NOT stack registry listeners (factory invariant)`**
- Asserts: open overlay → close → open again; `registry` listener count is `1`, not `2`.
- Test name: `installFocusedOverlayShortcut: dispose unsubscribes the registry listener`
- Asserts: post-dispose, listener count is `0`.

**Implementation (Green):**
- Move the `model.refresh()` call out of `FocusedStreamOverlay.render`.
- In `installFocusedOverlayShortcut`:
  - Register a `registry.on("change", () => { if (overlayOpen) model.refresh() })`
    listener at `install` time, idempotent.
  - Also call `model.refresh()` directly inside the keystroke dispatch wrapper
    (so navigation reflects the current registry state without waiting for the next
    change event).
- Factory keeps its no-listener invariant (no changes to factory other than test
  coverage).

**Acceptance criteria:**
- [ ] All listed assertions pass.
- [ ] **Re-opening overlay does NOT stack `RunRegistry` listeners (test pinned).** [factory invariant]
- [ ] `npm test` green; `npx tsc --noEmit` clean.
- [ ] No regression on cycle responsiveness — Tab navigation still feels live.
- [ ] Existing `tests/focused-overlay-factory.test.ts` (no-listener invariant) still
  passes unchanged.

**Risks:** Listener leak is the documented historic bug. Pin TWO tests (one in
shortcut.test, one in the new listener-leak.test) plus a manual smoke note in §7
(finalizer).

---

### Slice 12 — Backlog close + SHA citations

**Size:** S
**Depends on:** 0–11
**Files touched:** `docs/backlog.md`
**New tests:** —

**Sub-issue(s) addressed:** Final close-out for v0.8.1 Item 3.

**Pre-implementation verification:**
- `git log --oneline f38d557..HEAD` lists the 11 (or 12) prior commits to cite.
- Add a v0.9 candidate one-liner:
  *"`Box`-bordered overlay viewport (deferred from v0.8.3 Item 3 D4 #3 + §8)."*

**Implementation (Green):**
- Move v0.8.1 Item 3 entry to the CLOSED section.
- Cite SHAs of slices 0–11 with one-line subjects.
- Add the v0.9 candidate.

**Acceptance criteria:**
- [ ] `docs/backlog.md` updated.
- [ ] All cited SHAs resolve via `git show <sha>`.
- [ ] `npm test` green (no code change).
- [ ] `npx tsc --noEmit` clean (no code change).
- [ ] Pre-commit hook does NOT rebuild dist/ (docs-only; verify hook short-circuits).

**Risks:** Stale-SHA-via-amend (steering hook may force a message rewrite for slice
12 itself, invalidating its own SHA in any subsequent doc reference — not a problem
since slice 12 *is* the last commit).

---

## §6. Snapshot-thrash budget (slices 1–5)

Pre-allocated. Builders should expect to update these specific tests during the
listed slice, not be surprised by them.

| Slice | Test file:line | Test name | Why thrashed | Effort |
|---|---|---|---|---|
| 1 | `tests/transcript.test.ts:112` | "collapsed toolCall renders as a single chevron line" | Possibly affected by `shortenMiddle` if the bash literal trips it (unlikely with `"echo hi"`; verify) | None expected |
| 2 | `tests/transcript.test.ts:112` | (same) | Outcome line `↳ ✓` may now appear after the chevron line — fixture must add `isError: false` matched result OR pin `resultsByCallId` empty | 1 fixture tweak |
| 2 | `tests/transcript.test.ts:186` | "toolResult message renders indented under the call" | `collapseToolCalls: false` path unchanged; sanity-check pass | None expected |
| 3 | `tests/transcript.test.ts:150` | "thinking blocks are hidden by default" | New summary line `· thinking (N chars …)` now emitted by default | 1 assertion rewrite |
| 3 | `tests/foreground-stream.test.ts:120` | "thinking blocks are hidden" | Foreground now emits summary unconditionally | 1 assertion rewrite |
| 4 | `tests/transcript.test.ts:227` | "multiple assistant turns are separated by a turn header" | Regex `/turn\s+\d+/i` should still match `· turn 1` — verify; possibly relax to `/[─·]\s*turn/i` | 1 regex tweak |
| 4 | `tests/transcript.test.ts:51` | "single assistant text message becomes its text lines" | If `turnSeparator` previously emitted before single-turn body, drop is fine; verify | None expected |
| 4 | `tests/foreground-stream.test.ts:46` | "single assistant text turn renders header + that text" | Possibly affected by header-bottom-ruler drop in S7 (not S4) | None in S4 |
| 5b | `tests/transcript.test.ts:242` | "renderHeader: shows persona, id, status, elapsed" | Header now has trailing `· <activity>` segment for `running` runs | 1 regex broadening |
| 5b | `tests/transcript.test.ts:266` | "renderHeader: never exceeds the requested width" | Width discipline test — no behavior change expected, but pin a width=50 case | Audit only |
| 7 | `tests/transcript.test.ts:242` | (same as 5b) | Bottom-header ruler dropped per D3 | 1 regex tweak |
| 7 | `tests/foreground-stream.test.ts:46` | (same as 4) | Bottom-header ruler dropped | 1 assertion rewrite |
| 9 | `tests/transcript.test.ts:279,286,291,297` | renderFooter Esc/Tab/c/t/s hint tests | Hints relocate from `transcript.ts:FOOTER_HINTS` to `focused-stream-overlay.ts:FOOTER_BINDINGS` | 4 tests RELOCATED to overlay test file (not rewritten) |

**Total: ~10 of 34 existing tests** will see touch-up. Within oracle's 8–12 estimate.
Slice 9 has the largest single concentration (4 tests relocate) but it's a move, not
a logic rewrite.

---

## §7. Cumulative session metadata

### Slice + commit count

- **14 slices** (0, 1, 2, 3, 4, 5a, 5b, 6, 7, 8, 9, 10, 11, 12).
- **14 commits** — one per slice. Pre-commit hook auto-stages `dist/index.js` +
  `dist/index.js.map` for every slice that touches `src/` (slices 0–11, 5a, 5b ⇒ 13
  dist-rebuilding commits; slice 12 is docs-only).

### Builder ⇄ critic round-trips (1.0× nominal)

Default 1 round per slice. Flagged for ≥2 rounds based on oracle/inspector signals:

- **Slice 0** (1.5×) — blast-radius into `event-handler.ts`; `lastToolCall` byte-exact
  shape regression is the gotcha.
- **Slice 2** (1.5×) — branching on `isError` + throttle interaction; expect a
  preview-truncation tweak round.
- **Slice 5b** (1.5–2×) — width discipline + activity predicate corner cases;
  expect a width=50 regression round.
- **Slice 7** (2×) — architectural fork. Spike result + sentinel-vs-real-themeFn
  decision likely needs a critic pass to agree on the split.
- **Slice 11** (1.5×) — listener-leak invariant; expect a "stack two listeners"
  scenario the critic will demand a test for.

Total round budget: **14 nominal + ~5 extra rounds = ~19 builder turns**.

### Conservative cost ceiling for the implementation phase

(Following design budget pattern: builder ⇄ critic loops + verifier + finalizer,
similar shape to a refactor chain, opus on builder/critic/verifier/finalizer.)

| Phase | Cost (USD) |
|---|---|
| Slice 0 builder + critic | $4 |
| Slices 1–5b builder + critic (6 slices @ ~$2.5 avg, 5b heavier) | $16 |
| Slice 6 builder + critic | $2 |
| Slice 7 builder + critic ×2 | $7 |
| Slices 8–11 builder + critic (4 slices @ ~$2.5 avg, 11 heavier) | $11 |
| Slice 12 builder | $1 |
| Verifier (whole-task pass) | $4 |
| Finalizer (closer) | $2 |
| **Conservative ceiling** | **~$47** |

This is upper-bound. Plausible actual: $30–40 if no slice goes pathological. Above
the design's $25–30 estimate because of the 5a/5b split + S7 spike + S11 listener-leak
double-pin.

### Smoke-test list for finalizer

Dogfood checks that prove Item 3 is closed. Run these by spawning a real foreground
sub-agent (e.g. `inspector` on a small task) and inspecting the output:

1. **Sub-issue #1 fixed** — bash command `aws sts get-caller-identity --profile foo 2>/dev/null` renders with both `aws sts` head AND `tickety` or `/dev/null` tail visible (uses `shortenMiddle`).
2. **Sub-issue #2 fixed** — every collapsed `▸ bash` line has a sibling `↳ ✓` or `↳ ✗` outcome line.
3. **Sub-issue #3 fixed** — thinking blocks render as `· thinking (N chars / M lines)` summary by default; pressing `t` in Ctrl+G unfolds body.
4. **Sub-issue #4 + #6 fixed** — turn separators are single dim `· turn N` lines, no rulers; per-turn vertical chrome reduced ≥2 lines.
5. **Sub-issue #5 fixed** — header shows `· thinking` / `· $ bash` / `· idle 6s` instead of constant `Working...`.
6. **Sub-issue #7 fixed** — header glyph + persona name colored; tool lines first-char colored; `↳ ✓` green, `↳ ✗` red.
7. **Sub-issue #8 fixed** — bottom-header ruler dropped; header glyph color ties to first turn separator + tool-line first chars (visual cohesion).
8. **O2 fixed** — Ctrl+G overlay shows `↑ N hidden  ·  ↓ M hidden` scroll indicator when scrolled.
9. **O3 fixed** — Ctrl+G footer shows colored key hints (`[Esc] close  [Tab] cycle  [c] collapse  [t] thinking  [s] send`).
10. **O4 fixed** — Ctrl+G with no sub-agents shows centered dim prose; no flanking rulers.
11. **O5 fixed** — header status hue is success/error/accent/warning/muted matching status.
12. **O6 fixed** — Ctrl+G overlay re-opening does not leak `RunRegistry` listeners.
   Verify: `(registry as any).listenerCount?.("change")` is bounded across 5 open/close cycles.
13. **PRD Esc → detach preserved** — pressing Esc in foreground stream still detaches; pressing Esc in Ctrl+G overlay still closes overlay.
14. **Slice 0 lock** — `widget.ts` row text shape (`▶ bash $ git diff`) is byte-identical pre/post-redesign.

If any check fails, finalizer escalates to critic for a touch-up slice; do not close
v0.8.1 Item 3 until all 14 pass.

---

**Plan complete.** All 7 plan-spec sections included; 3 oracle amendments applied;
5a/5b split applied with `Run` investigation cited (preamble §A4); per-slice template
honored with size/deps/files/tests/sub-issue/verification/Red/Green/acceptance/risks;
oracle's 5 watch-fors pinned to the right slice acceptance criteria; ordering matches
load-bearing constraints; snapshot thrash pre-allocated; cost ceiling set conservatively.
