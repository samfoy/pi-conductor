# v0.8.2 backlog

Seeded from v0.8.1 Item 1 close (commit `423f500`, finalizer `finalizer-7794` ruling 2026-05-15).

## (B-4) Bedrock toolUse.name sanitizer — CLOSED 2026-05-15

Closed in commit `e9c0c1e`. Defense-in-depth in-memory
sanitizer for malformed `toolUse.name` (charset `[a-zA-Z0-9_-]+`) in the
parent session's outgoing context. Prevents the Bedrock validation wedge
witnessed in the `samfp/Rosie` session 2026-05-15. Pure function
`sanitizeToolNames` in `src/sanitizer.ts`, wired via `pi.on("context", …)`
in `src/index.ts` (extracted to `src/sanitizer-hook.ts` for testability —
mirrors the `installFocusedOverlayShortcut` precedent). JSONL on disk
untouched — original bytes preserved as audit evidence; sanitizer
self-heals on resume's first API call. Fully orthogonal to
`filterParentContext` (different surfaces, different invariants — pinned
by Fixture H). Investigation by `investigator-bdgz`, design by
`designer-w2a5`, oracle gate by `oracle-eur8`. Spec:
`docs/v0.8.2-b4-sanitizer-design.md` (migrated from repo-root `design.md`
per the v0.8.1 pattern).

The (A) `ensemble_kill` tool slice remains independent and is tracked
separately.

## (A) `ensemble_kill` tool — CLOSED 2026-05-15

Closed in commit `24b6e97`. Closes the asymmetry with `ensemble_pause` /
`ensemble_resume`. Thin wrapper around `forceTerminate(run, reason, registry)`
at `src/runs.ts:963–1003` for running/paused, and `SpawnQueue.removeQueued`
for queued sub-agents (the canonical queue cancellation path — splices
pending and forces the placeholder to terminal). Idempotent on terminal
states (returns success no-op so retries are safe). SIGTERM→SIGKILL on
processes; queue removal on queued sub-agents. Tool-initiated kills are
silent (no `onComplete` plumbed; no follow-up turn) per the pi-wide
convention documented for `subagent_kill`. Reduces (B-4) hallucination
trigger surface (the model can now reach for the real tool instead of
inventing it). 10 new tests in `tests/ensemble-kill.test.ts` mirror the
`ensemble-pause-resume.test.ts` shape (registration, unknown-id error,
three killable states, four idempotent terminal states, description
pins the silent-kill convention).

## P0 — workflow defects

### Parent-reload contract for context-filter / runs changes — CLOSED 2026-05-18

Closed by `1c72856` (`fix(lifecycle): preserve runs on /reload`) +
`71b42b4` (`build: rebuild dist/ for 1c72856 (lifecycle fix)`). The
host's `/reload` slash command + `ctx.reload()` API was already shipped
in `@earendil-works/pi-coding-agent`; the wedge was that pi-conductor's
`session_shutdown` handler treated reload identically to quit and
SIGTERM’d every running/paused run, defeating the host primitive.
Fix: branch on `event.reason === "reload"` in the new pure helper
`handleSessionShutdown` (`src/shutdown.ts`) — skip the kill loop, skip
`sanitizerHook.reset()`. 8 unit tests pin reload preservation +
quit/new/resume/fork tear-down behavior + only-running-or-paused-killed
+ swallowed-kill-throw + missing-`proc`-handle. Mutation-tested by
`critic-8bv5`: drop guard / call resetSanitizer on reload / SIGTERM→SIGKILL /
add queued-to-kill-predicate — each mutation reproduces a unit-test
failure.

What `/reload` now preserves: chat history, conductor brief, scratchpad
notes, the `pi.on("context", …)` sanitizer hook (re-registers cleanly).
What it does not preserve: in-flight sub-agent registry entries (the
new runtime has no record of them); surviving processes finish to
disk, but `<sub-agent-completed>` notifications across the runtime
swap don't fire. Registry rehydrate is a separate v0.9-class spike
(child stdout pipes can't survive runtime swap without re-attach work)
and is deliberately deferred. Iteration design / decision in
oracle-3l2e's review (`~/.pi/agent/conductor/runs/oracle-3l2e/final.md`).

Documentation of the new iteration workflow lives in
`CONTRIBUTING.md` — "Iteration workflow" section, added in the
dist-rebuild lifecycle gap close commit (see next entry).

### dist/ rebuild lifecycle gap — CLOSED 2026-05-18

Closed by the build(hooks) commit that auto-rebuilds `dist/` when
`src/` is staged. `hooks/pre-commit` now runs `npm run build --silent`
and `git add dist/index.js dist/index.js.map` ahead of the existing
`npm test` step, guarded by
`git diff --cached --name-only | grep -q '^src/'` so doc-/persona-/test-only
commits pay nothing. Build cost is ~30ms wall (esbuild) when fired.
Pinned by 4 tests in `tests/pre-commit-hook.test.ts` (src triggers
build + auto-stage; docs-only does not; `npm test` runs regardless;
build failure aborts before tests).

`CONTRIBUTING.md` documents `npm run dev` as the iteration default and
notes the hook is the safety net. `AGENTS.md` notes the hook handles
the rebuild so the green-bar list (`npm test`, `npx tsc --noEmit`)
stays unchanged. Decision context: oracle-d67w's review
(`~/.pi/agent/conductor/runs/oracle-d67w/final.md`).

Deferred follow-up (not blocked, not started): gitignoring `dist/`
entirely and leaning on `prepare: npm run build` for npm-published
tarballs. Structurally correct end-state but needs verification of the
`pi install npm:` consumer path before committing. Open as a v0.9
backlog item when a second consumer beyond the dogfooder appears.

## v0.8.1 Item 1 follow-ups

### §8.3 manual smoke gate — CLOSED 2026-05-15

Executed in fresh parent pi session post-restart (post `423f500`). Both
scenarios pass.

- **Scenario A (`critic`):** PASS. critic-tppr produced a structured review
  of the mock NaN-guard diff with no role confusion. Evidence quote:
  *"`if (Number.isNaN(a) || Number.isNaN(b)) return 0;` returns 0 iff at
  least one operand is NaN — matches 'either operand' exactly."* Transcript:
  `~/.pi/agent/conductor/runs/critic-tppr/`.

- **Scenario B (`clarifier`, substituted for `oracle` — see new item below):**
  PASS. clarifier-q5h2 produced 8 acceptance criteria + 7 targeted
  clarifying questions on the mock src/runs.ts → src/runs-gc.ts refactor
  plan, with no role confusion. Evidence quote: *"Step 1 says
  '`pruneOldRunRecords()` and helpers' — is the helper set defined by
  'every non-exported function transitively reachable from
  `pruneOldRunRecords`,' by a hand-picked list, or by file-section
  boundary?"* Transcript: `~/.pi/agent/conductor/runs/clarifier-q5h2/`.

- **Sentinel-presence check:** PASS. Both seeded slices begin with the §4.1
  verbatim sentinel ("Your brief is the LAST user-role message in this
  transcript" + "Some assistant prose may discuss YOU in the third person").

- **Structural-drop check:** PASS. `grep -c '"name":"ensemble_spawn"'` on
  both seeded slices returns 0 — every parent assistant turn that contained
  an `ensemble_spawn` toolCall was dropped whole, taking its orchestration
  prose with it. Surviving "Spawning critic" / "Holding the turn" hits in
  the slice are inside legitimate `toolResult` content (e.g. `read` of the
  design doc §8.3 itself, which quotes example narration as spec).

- **(a') reachability check (verifier-mandate novel):** PASS, recorded in
  verifier-xszh: `src/context-filter.ts:55` defines
  `DEFAULT_TOOL_PREFIXES = ["ensemble_", "subagent"]`;
  `"ensemble_spawn".startsWith("ensemble_") === true`; the drop branch at
  `src/context-filter.ts:104` is reachable from the spawn path used.

§8.3 carry-over from v0.8.1 Item 1 is fully discharged; v0.8.1 Item 1 is
done.

## v0.8.1 Item 1 design-doc patches (surfaced during §8.3 close)

### §8.3 Scenario B targets a persona that no longer reads filtered context

`docs/v0.8.1-item1-design.md` §5 (Q#16 audit) flips `oracle` to
`inherit_context: none`. `personas/oracle.md:inherit_context` confirms.
Therefore §8.3 Scenario B as written ("spawn an `oracle` against
pre-conditioned orchestration narration") cannot exercise the v0.8.1 fix —
oracle never sees the filtered slice and the structural drop has no effect
on what oracle reads. The design's §5↔§8.3 internal inconsistency was first
flagged by verifier-xszh.

Patch §8.3 Scenario B to target a still-`filtered` persona. The 2026-05-15
smoke run used `clarifier` and passed; recommend pinning `clarifier` (or
`investigator`) as the canonical Scenario B target. Small slice — design
doc edit + a one-line update to the §5 risk callout. No code change.

### §8.3 spec says "builder OR verifier may run"; sub-agents cannot

`docs/v0.8.1-item1-design.md` §8.3 reads *"the builder (or a verifier) must
run the following two scenarios end-to-end."* In practice, pi-conductor
sub-agents are blocked by a system-level constraint from calling
`ensemble_spawn` (verifier-xszh hit this: *"You are running as a
pi-conductor sub-agent. Do NOT attempt to spawn further sub-agents."*).

The live half of §8.3 can ONLY be executed by the overseer (parent)
directly. Patch §8.3 to drop "or a verifier" and document this constraint
explicitly. Verifier persona is still useful for the read-only static-half
checks (sentinel byte-identity, (a') reachability, tests, transcript
forensics) — verifier-xszh did exactly that — but cannot be the live-half
runner.

### Inverted comment at `tests/context-filter.test.ts:479` — CLOSED 2026-05-15

Closed in commit `e2ac8cf` (bundled with the §3.5 stopReason fix below).

Comment claimed preserving the non-excluded toolCall would create a
dangling toolResult orphan; the actual chosen invariant (drop the whole
turn) is the inverse — dropping orphans the surviving toolResult of the
non-excluded call. Runtime was correct; comment's reasoning was inverted.
Rewritten to accurately describe both choices' orphan implications and
name the load-bearing reason (orchestration-prose elimination per design
§3.1) for picking whole-turn drop.

### Stale `stopReason: "toolUse"` on `dropThinking`-only rewrite path — CLOSED 2026-05-15

Closed in commit `e2ac8cf`.

The `TODO(v0.8.2)` marker at `src/context-filter.ts:142–148` is removed.
The `dropThinking`-only rewrite branch now recomputes `stopReason`
conservatively: when the original was `"toolUse"` and no `toolCall`
remains in the filtered content, set `stopReason: "stop"` (the neutral
non-tool-use value per `@earendil-works/pi-ai`'s `StopReason` union);
otherwise pass through unchanged. Pinned by two new tests in
`tests/context-filter.test.ts` — one red-then-green for the stale-path
`[thinking, text]` shape, one preservation pin for the justified
`[thinking, toolCall]` shape (prevents regression to a too-aggressive
"always recompute" shape). See design `docs/v0.8.1-item1-design.md` §3.5.

### §11 verifier-brief sub-rule — CLOSED 2026-05-15

Closed in commit `1f70d08`. Per `docs/v0.8.1-item1-design.md` Q-followup block:
§11 now mandates that verifier briefs include the claimed change
explicitly, since `verifier` flipped to `inherit_context: none` and now
boots fresh. Implemented as a new "Verifier briefs MUST be self-contained"
paragraph in `src/conductor-prompt.ts` §11 (between the no-parallel-writes
and breaking-the-chain blocks), pinned by
`tests/conductor-prompt.test.ts` ("§11 — verifier briefs must be
self-contained").

## Other v0.8.1 items still open (not started)

- **Item 2** — parallel write-builder git collision (mitigation (a) shipped;
  (b) builder SHA-check guard and (c) harness-side cap still open).
- **Item 3** — foreground transcript view UX (8 documented issues; large
  slice, needs `inspector` → `designer` chain).
- **Item 4** — no-substantive-final-message harness guard (small slice).
- **Item 5** — context-inflation compaction (medium slice).

See `docs/v0.8.1-handoff-context.md` for full descriptions.

## Pre-existing drift — CLOSED 2026-05-15

Closed in commit `9c2780d` — the v0.7 decision-log entry at `PRD.md:574`
("Conductor mode ON by default") was annotated with `_Reversed in v0.8 —
see entry below._` to mirror the existing treatment at line 498. Section
retained for hand-off audit; remove on the next backlog rotation if no
new drift accrues.

`PRD.md:574` claims "ON by default" — v0.8 shipped OFF default in commit
`9becc65`. The PRD line is stale. Out of scope for v0.8.1 Item 1; flag for
whoever next edits the PRD locked-decisions section.

## Session observations (2026-05-15 v0.8.2 cleanup) — workflow papercuts / persona enhancements

*Surfaced while running the §8.3 close + small-slice cleanup queue (commits `f1420e1` through `69706eb`). Each is small; bundle freely or address one-off as convenient.*

### Scribe persona loop-verifies after commit; should return immediately

`scribe-g06v` (Group A doc patches; commits `f1420e1` + `9c2780d`) timed
out at the 30-minute walltime. The actual edits + commits landed by
roughly turn 21 (~minute 12); the remaining 18 minutes were spent
re-reading the committed files to "verify the final file states."

Cost: ~$1.09 of model time on already-committed content; UX cost of a
30m walltime with no useful incremental output past minute 12.

`scribe-76e8` (the §3.5-resolved + backlog-SHA-citation sweep) was
briefed explicitly *"return immediately after committing; no loop
verification"* and finished in 1.7 minutes for a comparable
two-file edit. The behavior change is achievable; the persona prompt
just needs the stop-condition pinned.

**Suggested slice:** edit `personas/scribe.md` to add an explicit
"after committing, return immediately — pre-commit hook is the
verification gate; do not re-read or re-diff already-committed
files" line. Add a test in `tests/personas.test.ts` (if a relevant
pin point exists) or pin via prompt-render check. Small builder slice.

### Steering hook rejects `§` in commit subjects (user-environment papercut)

Observed three times this session:

- `scribe-g06v` commit 1 (`f1420e1`): planned subject *"docs(design): patch §8.3 Scenario B target and runner constraint"*; landed as *"docs(design): patch section 8.3 Scenario B target and runner constraint"* after the steering hook rejected `§`.
- `builder-mpya` commit (`1f70d08`): planned *"feat(prompt): mandate self-contained verifier briefs in §11"*; landed as *"feat(prompt): mandate self-contained verifier briefs in section 11"*.
- `builder-f5el` and `scribe-76e8` were briefed to pre-substitute `§` → `section` proactively after observing the prior rejections; both landed clean on first attempt.

The steering hook is in the user's personal `~/.pi/` tooling, NOT a
pi-conductor surface — fixing the hook itself is out of scope for this
repo. But the friction is real and reproduces every time a sub-agent
commits about a numbered section.

**Suggested mitigation (in pi-conductor scope):** add a one-line note
to `personas/scribe.md` and `personas/builder.md` (or to the conductor
system prompt addendum's commit-message guidance) saying *"avoid
non-ASCII characters (`§`, `µ`, etc.) in commit subjects — some
steering hooks reject them; substitute spelled-out forms. Body text is
fine."* Pure prompt nudge, no code change.

**Out-of-scope alternative:** investigate whether the steering hook
should be relaxed for known-safe Unicode (`§`, em-dash, etc.). Not in
this repo. Filed for visibility only.

### Promote mutation-test pattern in critic persona's "novel verification"

`critic-29qx` (gating commit `e2ac8cf`) ran mutation testing as its
novel verification: `git stash` the source change, run the new
tests against pre-fix source, confirm the stale-path test fails red
(it did) AND the preservation pin still passes (it did). This is the
gold-standard TDD-evidence check — strongest possible confirmation
that the new tests really pin the new behavior, not just coincide
with it.

Current `personas/critic.md` lists "different inputs, different test
file, different harness, or a manual smoke path" as novel-check
examples but doesn't mention mutation testing. The pattern is more
load-bearing than the alternatives when the slice claim is "TDD
red→green for behavior X."

**Suggested slice:** edit `personas/critic.md` (and maybe
`personas/verifier.md`'s analogous novel-check guidance) to promote
mutation testing from "not mentioned" to "preferred when the slice
claims TDD coverage of new behavior." Phrase it as guidance, not a
mandate — some slices don't lend themselves to it (purely additive
features, doc edits, etc.).

Concrete addition for `personas/critic.md`:

> When the slice's claim is "TDD coverage of new behavior X," prefer
> a mutation test as your novel verification: `git stash` the source
> change, run the new tests against pre-fix source, confirm the
> red-step test actually goes red and the preservation/regression
> pins stay green. This is the strongest possible evidence that the
> tests pin the new behavior, not just coincide with it. If the
> slice is doc-only or purely additive, fall back to the other
> novel-check shapes.

Small builder slice; persona file + a prompt-render test if `tests/`
has the pin infrastructure.

### Stale SHA citations in backlog after `git commit --amend`

Three slices this session had their backlog CLOSED entries cite the
pre-amend SHA instead of the on-branch SHA:

- §11 verifier-brief sub-rule: `d9846ed` → actual `1f70d08`. Caught by
  `critic-rgbf`, fixed in `e2ac8cf`.
- (B-4) sanitizer: `2a37f6b` → actual `e9c0c1e`. Caught by
  `finalizer-gkke`, fixed in [this commit's SHA].
- (A) `ensemble_kill` tool: `045f0be` → actual `24b6e97`. Caught by
  `finalizer-gkke`, fixed in [this commit's SHA].

Each amend was triggered by the steering hook rejecting `§` in commit
subjects (separate session-observation item above) — the builder /
scribe edited the backlog with the pre-amend SHA, then the steering
hook forced an amend on the subject swap, leaving the backlog citation
pointing at an orphan.

Pattern recurred 3× in one session. Stale SHAs resolve today via
reflog (~30 day window) and silently break thereafter. Two mitigations:

- (a) Builder/scribe convention: update backlog SHA citations *after*
  the final amend, not before.
- (b) Reference commits in backlog by descriptive title or
  date-anchored phrase ("§11 verifier-brief slice, 2026-05-15") and
  derive SHA at lookup time via `git log --grep`.

(a) is convention-only; (b) is harder to enforce but more robust.

**Suggested slice:** edit `personas/builder.md` and `personas/scribe.md`
to add a "verify SHAs against final amend" step after commit; or add a
prepare-commit-msg hook that scans the staged backlog diff for stale
SHA references.


## Session observations (2026-05-18 v0.8.2 P0 close) — workflow papercuts

*Surfaced while closing the two P0 entries (parent-reload contract +
dist-rebuild lifecycle gap). Each is small; bundle freely.*

### Conventional Commits steering hook misfires on heredoc commit messages

`builder-mqye` (Step 1 lifecycle fix, commit `1c72856`) hit the user's
personal `~/.pi/` Conventional Commits steering hook regex while
attempting a multi-line message via `git commit -m "$(cat <<'EOF' ...`.
The regex inspects the literal `-m` arg and sees `$(cat...`, which
doesn't match the allowlist (`feat:` / `fix:` / etc.) — so the
attempt is rejected even though the actual subject is well-formed.

Workaround: write the message to a tempfile and use `git commit -F
/tmp/msg`. The `-F` path bypasses the `-m` regex entirely. This is a
user-environment papercut, not a pi-conductor surface — the hook
lives outside the repo. But the friction is reproducible across
sub-agents.

**Suggested mitigation (in pi-conductor scope):** one-line nudge in
`personas/builder.md` and `personas/scribe.md` — *"For multi-line
commit messages, prefer `git commit -F /tmp/msg` over heredoc-style
`git commit -m \"$(cat <<EOF ... EOF)\"` — some user-side steering
hooks reject the heredoc form."* Pure prompt nudge, no code change.

### Mutation testing substitutes for live smoke when smoke is structurally impossible

`critic-8bv5` (Step 1 lifecycle fix gate) and `verifier-zfqv`
(end-of-slice verification) both used mutation testing on
`src/shutdown.ts` as their novel verification: drop the reload guard,
flip SIGTERM→SIGKILL, etc. Each mutation reproduces a unit-test
failure, proving the tests pin the new behavior independently.

This was the strongest available runtime check because live
`/reload` smoke was structurally impossible — sub-agents are blocked
from executing host slash commands (a parent-only operation), and
the extension's `dist/` was deliberately stale ahead of the rebuild
commit. So the unit boundary was the only place the slice could be
exercised pre-merge.

The pattern generalizes: when live smoke is impossible (sub-agent
constraint, OS-level operation, network dependency) AND the slice
is TDD-shaped, mutation testing is the strongest substitute. The
existing `personas/critic.md` already endorses `git stash` mutation
for new-behavior coverage; this is the same shape with a different
motivation.

**Suggested addition to `personas/critic.md` and `personas/verifier.md`:**
note that mutation testing is also the recommended novel check when
*live* execution is structurally blocked (sub-agent can't trigger
host slash commands; slice modifies a runtime-only path that
requires extension reload; etc.), not just when the slice claim is
"TDD coverage of behavior X." Both motivations point at the same
technique. Could be bundled with the existing mutation-test
promotion observation above.

### Read-only personas should restore the working tree after build/measurement steps

`oracle-d67w` (dist-rebuild lifecycle gap framing) ran `npm run
build` to measure the rebuild diff for `1c72856`. The measurement
was the load-bearing finding (the framing's "two near-misses" claim
was actually three, with the third being HEAD). But oracle didn't
`git checkout -- dist/` after measuring, so the next `builder-v8q4`
inherited a dirty working tree it had to verify against the
documented diff before committing.

Net effect was correct (builder confirmed the dirty state matched
oracle's report exactly, then re-ran the build to confirm
idempotency before committing). But the verification step was extra
work caused by oracle leaving cleanup undone. Read-only personas
should leave the tree as they found it.

**Suggested mitigation:** one-line addition to `personas/oracle.md`
(and any other read-only persona that occasionally executes
build/measurement commands — `redteam`, `inspector`, `analyst`,
`profiler`, `investigator`) — *"if you execute a build/measurement
command that modifies tracked files, restore them with `git
checkout -- <paths>` before returning. Read-only personas should
leave the working tree as they found it."* Pure prompt nudge.
