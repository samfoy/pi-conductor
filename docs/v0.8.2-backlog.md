# v0.8.2 backlog

Seeded from v0.8.1 Item 1 close (commit `423f500`, finalizer `finalizer-7794` ruling 2026-05-15).

## v0.10 — Sub-agent watchdog — CLOSED 2026-05-20

Closed by 4 slice commits `c8623cb..4734298` on top of the v0.9 closure
base. Detects stalled sub-agents (silent for >Ns while `running` and not
paused) and surfaces a `<sub-agent-stalled>` advisory to the parent;
opt-in `kill_on_stall` per spawn escalates the hard threshold to
`forceTerminate`. All 972 tests pass at HEAD `4734298` (was 953 before
v0.10; +19 across the chain). Closes the witnessed hang class from
`critic-yjsn` / `builder-z98m` / orchestrator `npm test`.

Driven by the `inspector` → `designer` → `oracle` → builder/critic chain;
design in `docs/v0.10-watchdog-design.md` (now SHIPPED-banner-marked,
preserved as historical context).

### Sub-issue → SHA closure map

| Slice | Description | SHA | Commit subject |
|---|---|---|---|
| 1 | Pure detector + state types (`evaluateRun`, `WatchdogTransition`, `WatchdogState`, `WatchdogConfig`; `Run.stalledSince`) | `c8623cb` | `feat(watchdog): pure detector for run staleness with soft/hard thresholds` |
| 2 | Enforcer integration (`Watchdog` class, interval ticker, `formatStallNotification`, pre-kill recheck per design A2) | `b7a36a9` | `feat(watchdog): enforcer with pre-kill recheck and stall notification envelope` |
| 3 | Per-spawn config (`kill_on_stall`, `stall_threshold_seconds` on `ensemble_spawn` / `ensemble_send`; `resolveKillOnStall` helper, W1 mutation witness imports it directly per `docs/wdd.md` parallel-formula rule) | `9bed244` | `feat(watchdog): per-spawn kill_on_stall and stall_threshold_seconds overrides` |
| 4 | UX surfaces (widget `· STALLED Ns` glyph, `/conductor watchdog status` slash subcommand, doctor extension, ensemble-panel snapshot tests) | `4734298` | `feat(watchdog): UX surfaces for stalled sub-agents` |
| 5 | Backlog close + PRD update + design-doc archive marker | _(this commit)_ | docs-only; no `src/` change |

### Test-count delta

953 → 972 (+19) across the v0.10 chain. 6 skipped and 0 failing held
steady.

### Decision log entries locked

Five design questions Q1–Q5 (`docs/v0.10-watchdog-design.md` §7) land
in `PRD.md` decision log under the 2026-05-20 v0.10 watchdog entry:
`kill_on_stall` default OFF, soft 120s / hard 600s, 30s tick, soft
advisory triggers a turn, `Run.stalledSince` in-memory only.

### Off-script and fast-follow commits

- `2999fca` — `docs(wdd): add parallel-formula witness anti-pattern
  rule`. User-authored after slice 3's W1 critic gate uncovered a
  parallel-formula witness with no teeth; promoted into `docs/wdd.md`
  and cross-referenced from `personas/critic.md`.
- `533300e` — `chore(hooks): unset CONDUCTOR_SUBAGENT before pre-commit
  npm test`. Surfaced when slice-4 builder hit a sub-agent-context
  test path during pre-commit. One-line hook tweak.

## v0.9 Slice 1 — Legacy `extensions/conductor` symlink dual-load — CLOSED 2026-05-19

Fix shipped in this slice. Investigation: `docs/v0.9-symlink-investigation.md`.

Failure mode (briefly): pi-conductor loaded twice when both
`settings.packages[]` AND a legacy `~/.pi/agent/extensions/conductor/index.js`
symlink existed. The symlink-load resolved `import.meta.url` to the
symlink path, so `builtinPersonasDir()`'s `..` walk landed on
`~/.pi/agent/extensions/personas/` (nonexistent) instead of the real
package's `personas/` dir — silently returning 0 personas. Root cause
was the `mkdir -p ~/.pi/agent/extensions/conductor` + `ln -s` install
snippet documented in `README.md:65-71` and `HANDOVER.md`, cargo-culted
by agents following the README.

What landed in this slice:

- `src/personas.ts` — `builtinPersonasDir()` now canonicalizes
  `import.meta.url` via `realpathSync` before walking. Logic extracted
  into pure helper `resolveBuiltinPersonasDir(metaUrl)` for unit-test
  surface. Mutation-tested: removing `realpathSync` makes the new
  `resolveBuiltinPersonasDir: canonicalizes through legacy symlink layout`
  test fail.
- `src/doctor.ts` — added `## Legacy install path detected` section
  that fires when `~/.pi/agent/extensions/conductor/index.{js,ts}`
  exists. Defense-in-depth: the realpath fix prevents the failure;
  the doctor warning surfaces the config smell so users clean up.
  `DoctorReportOptions` gains an optional `homeDir` for stub-friendly
  testing.
- `README.md` — install snippet rewritten to use `settings.packages[]`
  and `pi -e /path/to/dist/index.js`. Explicit "do NOT symlink into
  extensions/conductor/" warning with link to the investigation doc.
- `HANDOVER.md` — moved to `docs/archive/HANDOVER-v0.7.md` (via
  `git mv` to preserve history). Archive header added marking it as
  v0.7-era with stale defaults.
- New tests (5 total): `resolveBuiltinPersonasDir: canonicalizes through
  legacy symlink layout`, `resolveBuiltinPersonasDir: passes through
  non-symlink paths unchanged`, `buildDoctorReport: warns on legacy
  ~/.pi/agent/extensions/conductor/index.js`, `buildDoctorReport: also
  warns on legacy index.ts entry`, `buildDoctorReport: omits
  legacy-install warning when no index entry exists`.

Upstream issue against `@earendil-works/pi-coding-agent` (auto-discovery
dedup by `realpath`) is investigation §6 open question — not filed here
per the user's standing preference (don't open upstream issues without
explicit instruction).

## v0.9 — Run-record GC capstone — CLOSED 2026-05-19

Closed by 8 slice commits `4f68833..fa5aff0` on top of v0.9 sprint base
`f1a73c6`, landing at HEAD `fa5aff0` with **874 tests / 868 passing /
6 skipped / 0 failing** (up from the 744-test base at the GC capstone
start — net +130 tests across the capstone alone, +214 since v0.9 sprint
start at 660). Closes PRD open-question #12 (resumable sessions for
finished sub-agents) and locks decisions D1–D8 in the PRD decision log.

Driven by the `inspector` → `designer` → `oracle` → builder/critic chain
mandated for capstone work; design artifacts:

- `docs/v0.9-gc-inspector-map.md` — disk-surface survey (228 runs / 11 GB
  on dogfood machine; transcripts are 98% of bytes; ≥10 transcripts
  ≥168 MB; orphaned `running` records observed). Source of D2 cap math.
- `docs/v0.9-gc-design.md` — the D1–D8 spec (dual policy, defaults, tier
  ladder, sidecar pinning, reconcile, dual trigger, ensemble_send safety,
  two-gate invariant) including 6 risks and unit / integration / mutation
  test strategy.
- `docs/v0.9-gc-oracle-review.md` — design-gate review. APPROVE WITH
  AMENDMENTS (A1 shutdown reconcile, A2 post-archive ensemble_send UX,
  A3 dry-run totals, A4 transcript-count correction). All four folded
  into slice acceptance.
- `docs/v0.9-gc-plan.md` — 9-slice execution plan; critic gates on
  slices 1, 2, 3, 5; scribe on 9.
- `docs/v0.9-gc-slice1-critic.md`, `docs/v0.9-gc-slice2-critic.md`,
  `docs/v0.9-gc-slice3-critic.md` — critic gate rulings. Slice 1 critic
  doc landed in slice 1; slice 2 + 3 critic docs landed in slice 6's
  closure window.

### Sub-issue → SHA closure map

| Slice | Description | SHA | Commit subject |
|---|---|---|---|
| 1 | Pure inventory + policy engine (D1, D2 defaults, D8 two-gate) | `4f68833` | `feat(gc): add pure inventory + policy engine for run-record reclaim` |
| 2 | Reconcile orphaned `running` records (D5; oracle A1 shutdown patch) | `9b2bc1c` | `feat(gc): reconcile orphaned running records on shutdown and via planner action` |
| 3 | Executor — cold-archive + delete with two-gate safety (D3, D7, D8) | `c613461` | `feat(gc): execute cold-archive and delete actions with two-gate safety` |
| 4 | User-pinning sidecar + `/conductor pin\|unpin` (D4) | `9060247` | `feat(gc): user-pinning sidecar with pin/unpin slash subcommands` |
| 5 | Orchestrator + `session_start` auto trigger with debounce (D6) | `def2256` | `feat(gc): orchestrator + session_start auto trigger with debounce` |
| 6 | Manual `/conductor gc` slash command — dry-run, persona, force, verbose (D6, oracle A3 four-totals) | `e0fb122` | `feat(gc): manual /conductor gc slash command with dry-run, persona, force, verbose` |
| 7 | Doctor disk-usage section + GC eviction preview | `d77afbf` | `feat(doctor): surface run-record disk usage and GC eviction preview` |
| 8 | History annotations — pinned + archived markers | `fa5aff0` | `feat(history): annotate runs with pinned and archived markers` |
| 9 | Backlog close + PRD update + A4 design correction | _(this commit)_ | docs-only; no `src/` change |

### v0.9 sprint commits before the GC capstone

Not part of the GC chain proper, but landed in the same v0.9 sprint window
and bring the test-count base to 744 before slice 1:

- `7d2f6c8` — `docs(wdd): name the mutation-witness pattern as Witness-Driven Development` (user-authored; captures the `git stash` + named-test discipline from v0.8.2 / v0.9). New `docs/wdd.md`; renames "Mutation test" → "Mutation witness" in `personas/critic.md`.
- `f1a73c6` — `feat(focused-overlay): per-agent scroll cue + complete wrapTextWithAnsi migration` (v0.8.3 §8 closeout; closes the two items deferred from Item 3).
- `0b7355f` — `feat(runs): cap concurrent write-capable spawns at 1 by default; require SHA check before history ops` (v0.8.1 Item 2).
- `b5fbd0c` — `feat(notifications): compact older sub-agent envelopes via context hook` (v0.8.1 Item 5).
- `0dfc2a2` — `feat(runs): warn on non-substantive final message at sub-agent completion` (v0.8.1 Item 4).
- `fe55ff3` — `feat(personas): apply v0.8.2 session-observation nudges to scribe, builder, critic` (closes the persona-nudge backlog: scribe return-immediately, builder verifier-brief sub-rule, critic mutation-witness promotion).
- `4488c30` — `fix(personas): canonicalize import.meta.url to survive legacy-extension symlinks` (root-cause fix from v0.9 Slice 1 / symlink-investigation).

### Test-count delta

660 → 874 (+214 total) across the v0.9 sprint; 744 → 874 (+130) across the
GC capstone alone. 6 skipped and 0 failing held steady.

### Decision log entries locked

All eight design decisions D1–D8 land in `PRD.md` decision log under the
2026-05-19 v0.9 GC capstone entry. PRD open-question #12 (resumable
sessions) marked Resolved with a pointer to the design and the per-decision
citations.

### Off-script and fast-follow commits

None. The 8-slice plan executed as designed; oracle amendments A1–A4 were
folded into slice acceptance criteria pre-implementation.

## (B-4) Bedrock toolUse.name sanitizer — CLOSED 2026-05-15

Closed in commit `e9c0c1e`. Defense-in-depth in-memory
sanitizer for malformed `toolUse.name` (charset `[a-zA-Z0-9_-]+`) in the
parent session's outgoing context. Prevents the Bedrock validation wedge
witnessed in the `samfp/Rosie` session 2026-05-15. Pure function
`sanitizeToolNames` in `src/sanitizer.ts`, wired via `pi.on("context", …)`
in `src/index.ts` (extracted to `src/sanitizer-hook.ts` for testability —
mirrors the `installFocusedOverlayShortcut` precedent). JSONL on disk
untouched — original bytes preserved as audit evidence; sanitizer
self-heals on resume's first API call. Fully orthogonal to
`filterParentContext` (different surfaces, different invariants — pinned
by Fixture H). Investigation by `investigator-bdgz`, design by
`designer-w2a5`, oracle gate by `oracle-eur8`. Spec:
`docs/v0.8.2-b4-sanitizer-design.md` (migrated from repo-root `design.md`
per the v0.8.1 pattern).

The (A) `ensemble_kill` tool slice remains independent and is tracked
separately.

## (A) `ensemble_kill` tool — CLOSED 2026-05-15

Closed in commit `24b6e97`. Closes the asymmetry with `ensemble_pause` /
`ensemble_resume`. Thin wrapper around `forceTerminate(run, reason, registry)`
at `src/runs.ts:963–1003` for running/paused, and `SpawnQueue.removeQueued`
for queued sub-agents (the canonical queue cancellation path — splices
pending and forces the placeholder to terminal). Idempotent on terminal
states (returns success no-op so retries are safe). SIGTERM→SIGKILL on
processes; queue removal on queued sub-agents. Tool-initiated kills are
silent (no `onComplete` plumbed; no follow-up turn) per the pi-wide
convention documented for `subagent_kill`. Reduces (B-4) hallucination
trigger surface (the model can now reach for the real tool instead of
inventing it). 10 new tests in `tests/ensemble-kill.test.ts` mirror the
`ensemble-pause-resume.test.ts` shape (registration, unknown-id error,
three killable states, four idempotent terminal states, description
pins the silent-kill convention).

## v0.8.3 candidates

### Per-call `timeout_minutes` on `ensemble_spawn` / `ensemble_send` + raise default 30 → 60 — CLOSED 2026-05-18

Closed by the `feat(tools): expose timeout_minutes on ensemble_spawn/send`
commit (descriptive self-reference per session-observation #4 stale-SHA-via-amend
mitigation). Motivated by `investigator-jtvf`'s trace of three Rosie-session
builder timeouts at exactly 30.0m: the ceiling lived in pi-conductor's own
`DEFAULT_CONFIG.defaultTimeoutMinutes` (`src/types.ts`) and the persona-
frontmatter fallback in `src/personas.ts`. Conductor had no per-call knob —
raising the budget for a single risky run required editing config or persona
files. Three changes:
  1. Bumped `DEFAULT_CONFIG.defaultTimeoutMinutes` and the persona-
     frontmatter fallback from 30 → 60.
  2. Added optional `timeout_minutes: integer in [1, 1440]` to both
     `ensemble_spawn` and `ensemble_send` tool schemas (TypeBox
     `Type.Integer({minimum: 1, maximum: 1440})`).
  3. Tool-arg layers on top of the cascade as `ov.timeoutMinutes`,
     so per-call > per-persona override > persona frontmatter > global
     default. Defensive runtime range-check (`validateTimeoutMinutes`)
     fires before persona resolution / queue dispatch / sendToRun —
     hosts that don't enforce schema bounds still get an early errorResult.

Wiring: spawn-path resolves `ov` via `{...baseOv, timeoutMinutes: params.timeout_minutes}`
before `resolveTimeoutMs(persona, ov, cfg)` (`src/tools.ts:registerSpawnTool`).
Send-path mirrors it (`registerSendTool`) — send arms a fresh wall-clock
budget per call, observed empirically when `builder-tiev` ran 33.2m via
`ensemble_send` in the v0.8.2 P0 close session.

Corresponding doc updates: `PRD.md` (table default 30 → 60, persona snippet,
`config.json` example), `README.md` (persona snippet + config example).
New tests in `tests/timeout-override.test.ts` pin: default constant = 60,
persona-fallback = 60, both schemas declare optional `timeout_minutes`
in [1, 1440], spawn-arg propagates as `opts.timeoutMs` via a fake-queue
capture, omitting falls back to 60m, and out-of-range (0 / 1441 / -3)
returns `errorResult` without invoking the queue or `sendToRun`.

## P0 — workflow defects

### Parent-reload contract for context-filter / runs changes — CLOSED 2026-05-18

Closed by `1c72856` (`fix(lifecycle): preserve runs on /reload`) +
`71b42b4` (`build: rebuild dist/ for 1c72856 (lifecycle fix)`). The
host's `/reload` slash command + `ctx.reload()` API was already shipped
in `@earendil-works/pi-coding-agent`; the wedge was that pi-conductor's
`session_shutdown` handler treated reload identically to quit and
SIGTERM’d every running/paused run, defeating the host primitive.
Fix: branch on `event.reason === "reload"` in the new pure helper
`handleSessionShutdown` (`src/shutdown.ts`) — skip the kill loop, skip
`sanitizerHook.reset()`. 8 unit tests pin reload preservation +
quit/new/resume/fork tear-down behavior + only-running-or-paused-killed
+ swallowed-kill-throw + missing-`proc`-handle. Mutation-tested by
`critic-8bv5`: drop guard / call resetSanitizer on reload / SIGTERM→SIGKILL /
add queued-to-kill-predicate — each mutation reproduces a unit-test
failure.

What `/reload` now preserves: chat history, conductor brief, scratchpad
notes, the `pi.on("context", …)` sanitizer hook (re-registers cleanly).
What it does not preserve: in-flight sub-agent registry entries (the
new runtime has no record of them); surviving processes finish to
disk, but `<sub-agent-completed>` notifications across the runtime
swap don't fire. Registry rehydrate is a separate v0.9-class spike
(child stdout pipes can't survive runtime swap without re-attach work)
and is deliberately deferred. Iteration design / decision in
oracle-3l2e's review (`~/.pi/agent/conductor/runs/oracle-3l2e/final.md`).

Documentation of the new iteration workflow lives in
`CONTRIBUTING.md` — "Iteration workflow" section, added in the
dist-rebuild lifecycle gap close commit (see next entry).

### dist/ rebuild lifecycle gap — CLOSED 2026-05-18

Closed by the build(hooks) commit that auto-rebuilds `dist/` when
`src/` is staged. `hooks/pre-commit` now runs `npm run build --silent`
and `git add dist/index.js dist/index.js.map` ahead of the existing
`npm test` step, guarded by
`git diff --cached --name-only | grep -q '^src/'` so doc-/persona-/test-only
commits pay nothing. Build cost is ~30ms wall (esbuild) when fired.
Pinned by 4 tests in `tests/pre-commit-hook.test.ts` (src triggers
build + auto-stage; docs-only does not; `npm test` runs regardless;
build failure aborts before tests).

`CONTRIBUTING.md` documents `npm run dev` as the iteration default and
notes the hook is the safety net. `AGENTS.md` notes the hook handles
the rebuild so the green-bar list (`npm test`, `npx tsc --noEmit`)
stays unchanged. Decision context: oracle-d67w's review
(`~/.pi/agent/conductor/runs/oracle-d67w/final.md`).

Deferred follow-up (not blocked, not started): gitignoring `dist/`
entirely and leaning on `prepare: npm run build` for npm-published
tarballs. Structurally correct end-state but needs verification of the
`pi install npm:` consumer path before committing. Open as a v0.9
backlog item when a second consumer beyond the dogfooder appears.

## v0.8.1 Item 1 follow-ups

### §8.3 manual smoke gate — CLOSED 2026-05-15

Executed in fresh parent pi session post-restart (post `423f500`). Both
scenarios pass.

- **Scenario A (`critic`):** PASS. critic-tppr produced a structured review
  of the mock NaN-guard diff with no role confusion. Evidence quote:
  *"`if (Number.isNaN(a) || Number.isNaN(b)) return 0;` returns 0 iff at
  least one operand is NaN — matches 'either operand' exactly."* Transcript:
  `~/.pi/agent/conductor/runs/critic-tppr/`.

- **Scenario B (`clarifier`, substituted for `oracle` — see new item below):**
  PASS. clarifier-q5h2 produced 8 acceptance criteria + 7 targeted
  clarifying questions on the mock src/runs.ts → src/runs-gc.ts refactor
  plan, with no role confusion. Evidence quote: *"Step 1 says
  '`pruneOldRunRecords()` and helpers' — is the helper set defined by
  'every non-exported function transitively reachable from
  `pruneOldRunRecords`,' by a hand-picked list, or by file-section
  boundary?"* Transcript: `~/.pi/agent/conductor/runs/clarifier-q5h2/`.

- **Sentinel-presence check:** PASS. Both seeded slices begin with the §4.1
  verbatim sentinel ("Your brief is the LAST user-role message in this
  transcript" + "Some assistant prose may discuss YOU in the third person").

- **Structural-drop check:** PASS. `grep -c '"name":"ensemble_spawn"'` on
  both seeded slices returns 0 — every parent assistant turn that contained
  an `ensemble_spawn` toolCall was dropped whole, taking its orchestration
  prose with it. Surviving "Spawning critic" / "Holding the turn" hits in
  the slice are inside legitimate `toolResult` content (e.g. `read` of the
  design doc §8.3 itself, which quotes example narration as spec).

- **(a') reachability check (verifier-mandate novel):** PASS, recorded in
  verifier-xszh: `src/context-filter.ts:55` defines
  `DEFAULT_TOOL_PREFIXES = ["ensemble_", "subagent"]`;
  `"ensemble_spawn".startsWith("ensemble_") === true`; the drop branch at
  `src/context-filter.ts:104` is reachable from the spawn path used.

§8.3 carry-over from v0.8.1 Item 1 is fully discharged; v0.8.1 Item 1 is
done.

## v0.8.1 Item 1 design-doc patches (surfaced during §8.3 close)

### §8.3 Scenario B targets a persona that no longer reads filtered context

`docs/v0.8.1-item1-design.md` §5 (Q#16 audit) flips `oracle` to
`inherit_context: none`. `personas/oracle.md:inherit_context` confirms.
Therefore §8.3 Scenario B as written ("spawn an `oracle` against
pre-conditioned orchestration narration") cannot exercise the v0.8.1 fix —
oracle never sees the filtered slice and the structural drop has no effect
on what oracle reads. The design's §5↔§8.3 internal inconsistency was first
flagged by verifier-xszh.

Patch §8.3 Scenario B to target a still-`filtered` persona. The 2026-05-15
smoke run used `clarifier` and passed; recommend pinning `clarifier` (or
`investigator`) as the canonical Scenario B target. Small slice — design
doc edit + a one-line update to the §5 risk callout. No code change.

### §8.3 spec says "builder OR verifier may run"; sub-agents cannot

`docs/v0.8.1-item1-design.md` §8.3 reads *"the builder (or a verifier) must
run the following two scenarios end-to-end."* In practice, pi-conductor
sub-agents are blocked by a system-level constraint from calling
`ensemble_spawn` (verifier-xszh hit this: *"You are running as a
pi-conductor sub-agent. Do NOT attempt to spawn further sub-agents."*).

The live half of §8.3 can ONLY be executed by the overseer (parent)
directly. Patch §8.3 to drop "or a verifier" and document this constraint
explicitly. Verifier persona is still useful for the read-only static-half
checks (sentinel byte-identity, (a') reachability, tests, transcript
forensics) — verifier-xszh did exactly that — but cannot be the live-half
runner.

### Inverted comment at `tests/context-filter.test.ts:479` — CLOSED 2026-05-15

Closed in commit `e2ac8cf` (bundled with the §3.5 stopReason fix below).

Comment claimed preserving the non-excluded toolCall would create a
dangling toolResult orphan; the actual chosen invariant (drop the whole
turn) is the inverse — dropping orphans the surviving toolResult of the
non-excluded call. Runtime was correct; comment's reasoning was inverted.
Rewritten to accurately describe both choices' orphan implications and
name the load-bearing reason (orchestration-prose elimination per design
§3.1) for picking whole-turn drop.

### Stale `stopReason: "toolUse"` on `dropThinking`-only rewrite path — CLOSED 2026-05-15

Closed in commit `e2ac8cf`.

The `TODO(v0.8.2)` marker at `src/context-filter.ts:142–148` is removed.
The `dropThinking`-only rewrite branch now recomputes `stopReason`
conservatively: when the original was `"toolUse"` and no `toolCall`
remains in the filtered content, set `stopReason: "stop"` (the neutral
non-tool-use value per `@earendil-works/pi-ai`'s `StopReason` union);
otherwise pass through unchanged. Pinned by two new tests in
`tests/context-filter.test.ts` — one red-then-green for the stale-path
`[thinking, text]` shape, one preservation pin for the justified
`[thinking, toolCall]` shape (prevents regression to a too-aggressive
"always recompute" shape). See design `docs/v0.8.1-item1-design.md` §3.5.

### §11 verifier-brief sub-rule — CLOSED 2026-05-15

Closed in commit `1f70d08`. Per `docs/v0.8.1-item1-design.md` Q-followup block:
§11 now mandates that verifier briefs include the claimed change
explicitly, since `verifier` flipped to `inherit_context: none` and now
boots fresh. Implemented as a new "Verifier briefs MUST be self-contained"
paragraph in `src/conductor-prompt.ts` §11 (between the no-parallel-writes
and breaking-the-chain blocks), pinned by
`tests/conductor-prompt.test.ts` ("§11 — verifier briefs must be
self-contained").

## v0.8.1 Item 3 — Foreground transcript view UX [CLOSED 2026-05-18]

Closed by 14 commits on top of base `f38d557`, landing at HEAD `c7b1340`
with 655 tests / 649 passing / 6 skipped / 0 failing (up from the
490/484/6/0 baseline at slice 0 — net +165 tests, +165 passing).
Driven by the `inspector` → `designer` → `oracle` → builder/critic/
verifier chain mandated in the v0.8.1 hand-off; design artifacts:

- `docs/v0.8.3-item3-inspector-map.md` — code-surface map
- `docs/v0.8.3-item3-design.md` — redesign spec (the slice plan's
  source of truth, including §8 deferrals and amendment 1)
- `docs/v0.8.3-item3-oracle-review.md` — design-gate review
- `docs/v0.8.3-item3-plan.md` — 14-slice execution plan (13 build slices + slice 12 backlog close; slice 5 split into 5a/5b)

### Sub-issue → SHA closure map

The 8 v0.8.1-documented foreground-transcript issues:

1. **Truncation cuts wrong place** — `67ca34f` (`feat(transcript):
   truncate bash tool calls with shortenMiddle to surface argument
   tail`, Slice 1). Same commit covers the widget per design option (b).
2. **Tool results invisible** — `100fffa` (`feat(transcript): show
   tool-call outcome glyph and preview in collapsed mode`, Slice 2).
3. **Thinking hidden, no hint** — `36302a0` (`feat(transcript):
   summarize hidden thinking blocks with char/line count`, Slice 3).
4. **Flat hierarchy** — `88775d2` (`feat(transcript): replace
   turn-separator rules with single-line marker`, Slice 4) +
   `fc7d067` (component-layer color, Slice 7).
5. **Constant `Working...`** — `d9df538` (`feat(transcript): show
   live activity in header instead of constant Working`, Slice 5b)
   on top of `95c8258` (`feat(runs): track lastEventAt on every event
   push`, Slice 5a infra).
6. **Vertical waste** — `88775d2` (drop turn-separator rules) +
   `fc7d067` (drop bottom-header ruler).
7. **Monochrome** — `fc7d067` (`feat(transcript): style transcript
   output via component-layer theme application`, Slice 7).
8. **Header-line disconnect** — `fc7d067` (drops the divider ruler
   and shares the accent palette across header + body).

The 7 Ctrl+G focused-overlay sub-issues (O1–O7) surfaced during
design:

- **O1 redundant rulers** — `88775d2` + `fc7d067`.
- **O2 no scroll indicator** — `68345da` (`feat(focused-overlay):
  show scroll-position hint above footer`, Slice 8).
- **O3 monochrome footer** — `20695f9` (`feat(focused-overlay):
  unify footer bindings and style key hints`, Slice 9).
- **O4 ugly empty state** — `6bbf0fb` (`feat(focused-overlay):
  polish empty-state with dim prose and dropped rulers`, Slice 10).
- **O5 status-blind header** — `fc7d067` (component-layer styling
  shares the run-status palette).
- **O6 render side-effect** — `c7b1340` (`refactor(focused-overlay):
  make render pure; move refresh to dispatch and registry listener`,
  Slice 11).
- **O7 footer/dispatch decoupled** — `20695f9` (footer bindings
  unified with dispatch).

### Off-script and fast-follow commits

- **`6793a5a`** — `fix(transcript): measure wrap/truncate widths with
  pi-tui visibleWidth`. Off-script pre-fix landed before Slice 0;
  approved by Slice 0 critic. Migrated transcript width measurement
  off the local approximations onto the pi-tui `visibleWidth`
  primitive — precondition for Slice 7's component-layer styling
  fork (theme-applied output contains ANSI escapes the local width
  helper miscounted). Without it, Slice 7's accent-color rendering
  would have produced wrong-width truncations.
- **`1c256f1`** — `refactor(transcript): finish STATUS_GLYPH dedup in
  history and commands`. F1 fast-follow on Slice 0 (`1e4f035`,
  `refactor(transcript): consolidate STATUS_GLYPH and tool-summary
  helpers`) — completed the dedup leak in the history and
  command-rendering paths that Slice 0 left half-migrated.

### Test-count delta

490 → 655 (+165 total); 484 passing → 649 passing (+165). 6 skipped
and 0 failing held steady across the slice run.

### Deferred to v0.9 (per design §8 and amendment 1)

- **`Box`-bordered overlay viewport** — deferred per design §8 /
  amendment 1. The overlay continues to render as flat lines; box
  layout was scoped out to keep the slice plan tractable. **Still
  deferred** — needs a design pass on the pi-tui `Box` API surface
  before implementation.
- **Per-agent scroll-cycle annotation** — ~~deferred per design
  §8~~ **CLOSED 2026-05-19** in v0.9 closeout commit (extends
  `renderScrollHint` with optional `agentContext`; multi-agent
  overlays append `<id> (line K/M)` breadcrumb after a Tab cycle;
  single-agent overlays unchanged). The classifier picks up the new
  shape so it dims with the rest of the scroll-hint kind.
- **Migrating `wrap()` to `wrapTextWithAnsi` wholesale** — ~~partially
  done in `6793a5a` (the width-measurement primitive)~~ **CLOSED
  2026-05-19** in v0.9 closeout commit. Audit confirmed: the only
  `wrap()` callsites in `src/` are in `transcript.ts` and both call
  *before* `applyTheme` injects ANSI; the helper itself is already a
  thin `wrapTextWithAnsi` delegate (post-`6793a5a`). The orthogonal
  `clip()` helper in `focused-stream-overlay.ts` was migrated to
  `truncateToWidth` + `visibleWidth` for defensive consistency. A new
  `tests/transcript-style.test.ts` regression pins the invariant that
  ANSI-styled lines re-wrapped via `wrapTextWithAnsi` preserve
  `visibleWidth(line) ≤ width`.

## Other v0.8.1 items still open (not started)

- **Item 2** — parallel write-builder git collision (mitigation (a) shipped;
  (b) builder SHA-check guard and (c) harness-side cap still open).
- **Item 3** — foreground transcript view UX — CLOSED 2026-05-18 (see
  dedicated section below).
- **Item 4** — no-substantive-final-message harness guard (small slice).
- **Item 5** — context-inflation compaction (medium slice).

See `docs/v0.8.1-handoff-context.md` for full descriptions.

## Pre-existing drift — CLOSED 2026-05-15

Closed in commit `9c2780d` — the v0.7 decision-log entry at `PRD.md:574`
("Conductor mode ON by default") was annotated with `_Reversed in v0.8 —
see entry below._` to mirror the existing treatment at line 498. Section
retained for hand-off audit; remove on the next backlog rotation if no
new drift accrues.

`PRD.md:574` claims "ON by default" — v0.8 shipped OFF default in commit
`9becc65`. The PRD line is stale. Out of scope for v0.8.1 Item 1; flag for
whoever next edits the PRD locked-decisions section.

## Session observations (2026-05-15 v0.8.2 cleanup) — workflow papercuts / persona enhancements — CLOSED 2026-05-19

All four observations below addressed in a single persona-prompt commit. See "Closed by" rows; persona files (`personas/scribe.md`, `personas/builder.md`, `personas/critic.md`) updated; no test changes needed (frontmatter shape unchanged; `tests/personas.test.ts` still green).

*Surfaced while running the §8.3 close + small-slice cleanup queue (commits `f1420e1` through `69706eb`). Each is small; bundle freely or address one-off as convenient.*

### Scribe persona loop-verifies after commit; should return immediately — CLOSED 2026-05-19

**Closed by:** `personas/scribe.md` gained a new "On finishing" section pinning the explicit "return immediately; pre-commit hook is the verification gate" stop-condition. See commit `feat(personas): apply v0.8.2 session-observation nudges to scribe, builder, critic`.

`scribe-g06v` (Group A doc patches; commits `f1420e1` + `9c2780d`) timed
out at the 30-minute walltime. The actual edits + commits landed by
roughly turn 21 (~minute 12); the remaining 18 minutes were spent
re-reading the committed files to "verify the final file states."

Cost: ~$1.09 of model time on already-committed content; UX cost of a
30m walltime with no useful incremental output past minute 12.

`scribe-76e8` (the §3.5-resolved + backlog-SHA-citation sweep) was
briefed explicitly *"return immediately after committing; no loop
verification"* and finished in 1.7 minutes for a comparable
two-file edit. The behavior change is achievable; the persona prompt
just needs the stop-condition pinned.

**Suggested slice:** edit `personas/scribe.md` to add an explicit
"after committing, return immediately — pre-commit hook is the
verification gate; do not re-read or re-diff already-committed
files" line. Add a test in `tests/personas.test.ts` (if a relevant
pin point exists) or pin via prompt-render check. Small builder slice.

### Steering hook rejects `§` in commit subjects (user-environment papercut) — CLOSED 2026-05-19

**Closed by:** `personas/scribe.md` and `personas/builder.md` each gained a "Commit format" section warning against `§` (and other non-ASCII characters: `µ`, em-dash) in commit subjects, with the spelled-out substitution rule. The user-side hook itself remains out of scope — this closes the conductor side. See commit `feat(personas): apply v0.8.2 session-observation nudges to scribe, builder, critic`.

Observed three times this session:

- `scribe-g06v` commit 1 (`f1420e1`): planned subject *"docs(design): patch §8.3 Scenario B target and runner constraint"*; landed as *"docs(design): patch section 8.3 Scenario B target and runner constraint"* after the steering hook rejected `§`.
- `builder-mpya` commit (`1f70d08`): planned *"feat(prompt): mandate self-contained verifier briefs in §11"*; landed as *"feat(prompt): mandate self-contained verifier briefs in section 11"*.
- `builder-f5el` and `scribe-76e8` were briefed to pre-substitute `§` → `section` proactively after observing the prior rejections; both landed clean on first attempt.

The steering hook is in the user's personal `~/.pi/` tooling, NOT a
pi-conductor surface — fixing the hook itself is out of scope for this
repo. But the friction is real and reproduces every time a sub-agent
commits about a numbered section.

**Suggested mitigation (in pi-conductor scope):** add a one-line note
to `personas/scribe.md` and `personas/builder.md` (or to the conductor
system prompt addendum's commit-message guidance) saying *"avoid
non-ASCII characters (`§`, `µ`, etc.) in commit subjects — some
steering hooks reject them; substitute spelled-out forms. Body text is
fine."* Pure prompt nudge, no code change.

**Out-of-scope alternative:** investigate whether the steering hook
should be relaxed for known-safe Unicode (`§`, em-dash, etc.). Not in
this repo. Filed for visibility only.

### Promote mutation-test pattern in critic persona's "novel verification" — CLOSED 2026-05-19

**Closed by:** `personas/critic.md` "Mandatory novel verification" section gained a new bullet promoting the `git stash` mutation-test pattern as the gold-standard novel verification when the slice claims TDD coverage of new behavior. See commit `feat(personas): apply v0.8.2 session-observation nudges to scribe, builder, critic`.

`critic-29qx` (gating commit `e2ac8cf`) ran mutation testing as its
novel verification: `git stash` the source change, run the new
tests against pre-fix source, confirm the stale-path test fails red
(it did) AND the preservation pin still passes (it did). This is the
gold-standard TDD-evidence check — strongest possible confirmation
that the new tests really pin the new behavior, not just coincide
with it.

Current `personas/critic.md` lists "different inputs, different test
file, different harness, or a manual smoke path" as novel-check
examples but doesn't mention mutation testing. The pattern is more
load-bearing than the alternatives when the slice claim is "TDD
red→green for behavior X."

**Suggested slice:** edit `personas/critic.md` (and maybe
`personas/verifier.md`'s analogous novel-check guidance) to promote
mutation testing from "not mentioned" to "preferred when the slice
claims TDD coverage of new behavior." Phrase it as guidance, not a
mandate — some slices don't lend themselves to it (purely additive
features, doc edits, etc.).

Concrete addition for `personas/critic.md`:

> When the slice's claim is "TDD coverage of new behavior X," prefer
> a mutation test as your novel verification: `git stash` the source
> change, run the new tests against pre-fix source, confirm the
> red-step test actually goes red and the preservation/regression
> pins stay green. This is the strongest possible evidence that the
> tests pin the new behavior, not just coincide with it. If the
> slice is doc-only or purely additive, fall back to the other
> novel-check shapes.

Small builder slice; persona file + a prompt-render test if `tests/`
has the pin infrastructure.

### Stale SHA citations in backlog after `git commit --amend` — CLOSED 2026-05-19

**Closed by:** dual mitigation landed. `personas/scribe.md` "Commit format" section now requires citing commits by descriptive subject + short SHA (not SHA alone). Both `personas/scribe.md` and `personas/builder.md` gained a post-amend SHA-sweep step using `git reflog | head` to find the pre-amend SHA. See commit `feat(personas): apply v0.8.2 session-observation nudges to scribe, builder, critic`.

Three slices this session had their backlog CLOSED entries cite the
pre-amend SHA instead of the on-branch SHA:

- §11 verifier-brief sub-rule: `d9846ed` → actual `1f70d08`. Caught by
  `critic-rgbf`, fixed in `e2ac8cf`.
- (B-4) sanitizer: `2a37f6b` → actual `e9c0c1e`. Caught by
  `finalizer-gkke`, fixed in [this commit's SHA].
- (A) `ensemble_kill` tool: `045f0be` → actual `24b6e97`. Caught by
  `finalizer-gkke`, fixed in [this commit's SHA].

Each amend was triggered by the steering hook rejecting `§` in commit
subjects (separate session-observation item above) — the builder /
scribe edited the backlog with the pre-amend SHA, then the steering
hook forced an amend on the subject swap, leaving the backlog citation
pointing at an orphan.

Pattern recurred 3× in one session. Stale SHAs resolve today via
reflog (~30 day window) and silently break thereafter. Two mitigations:

- (a) Builder/scribe convention: update backlog SHA citations *after*
  the final amend, not before.
- (b) Reference commits in backlog by descriptive title or
  date-anchored phrase ("§11 verifier-brief slice, 2026-05-15") and
  derive SHA at lookup time via `git log --grep`.

(a) is convention-only; (b) is harder to enforce but more robust.

**Suggested slice:** edit `personas/builder.md` and `personas/scribe.md`
to add a "verify SHAs against final amend" step after commit; or add a
prepare-commit-msg hook that scans the staged backlog diff for stale
SHA references.


## Session observations (2026-05-18 v0.8.2 P0 close) — workflow papercuts

*Surfaced while closing the two P0 entries (parent-reload contract +
dist-rebuild lifecycle gap). Each is small; bundle freely.*

### Conventional Commits steering hook misfires on heredoc commit messages

`builder-mqye` (Step 1 lifecycle fix, commit `1c72856`) hit the user's
personal `~/.pi/` Conventional Commits steering hook regex while
attempting a multi-line message via `git commit -m "$(cat <<'EOF' ...`.
The regex inspects the literal `-m` arg and sees `$(cat...`, which
doesn't match the allowlist (`feat:` / `fix:` / etc.) — so the
attempt is rejected even though the actual subject is well-formed.

Workaround: write the message to a tempfile and use `git commit -F
/tmp/msg`. The `-F` path bypasses the `-m` regex entirely. This is a
user-environment papercut, not a pi-conductor surface — the hook
lives outside the repo. But the friction is reproducible across
sub-agents.

**Suggested mitigation (in pi-conductor scope):** one-line nudge in
`personas/builder.md` and `personas/scribe.md` — *"For multi-line
commit messages, prefer `git commit -F /tmp/msg` over heredoc-style
`git commit -m \"$(cat <<EOF ... EOF)\"` — some user-side steering
hooks reject the heredoc form."* Pure prompt nudge, no code change.

### Mutation testing substitutes for live smoke when smoke is structurally impossible

`critic-8bv5` (Step 1 lifecycle fix gate) and `verifier-zfqv`
(end-of-slice verification) both used mutation testing on
`src/shutdown.ts` as their novel verification: drop the reload guard,
flip SIGTERM→SIGKILL, etc. Each mutation reproduces a unit-test
failure, proving the tests pin the new behavior independently.

This was the strongest available runtime check because live
`/reload` smoke was structurally impossible — sub-agents are blocked
from executing host slash commands (a parent-only operation), and
the extension's `dist/` was deliberately stale ahead of the rebuild
commit. So the unit boundary was the only place the slice could be
exercised pre-merge.

The pattern generalizes: when live smoke is impossible (sub-agent
constraint, OS-level operation, network dependency) AND the slice
is TDD-shaped, mutation testing is the strongest substitute. The
existing `personas/critic.md` already endorses `git stash` mutation
for new-behavior coverage; this is the same shape with a different
motivation.

**Suggested addition to `personas/critic.md` and `personas/verifier.md`:**
note that mutation testing is also the recommended novel check when
*live* execution is structurally blocked (sub-agent can't trigger
host slash commands; slice modifies a runtime-only path that
requires extension reload; etc.), not just when the slice claim is
"TDD coverage of behavior X." Both motivations point at the same
technique. Could be bundled with the existing mutation-test
promotion observation above.

### Read-only personas should restore the working tree after build/measurement steps

`oracle-d67w` (dist-rebuild lifecycle gap framing) ran `npm run
build` to measure the rebuild diff for `1c72856`. The measurement
was the load-bearing finding (the framing's "two near-misses" claim
was actually three, with the third being HEAD). But oracle didn't
`git checkout -- dist/` after measuring, so the next `builder-v8q4`
inherited a dirty working tree it had to verify against the
documented diff before committing.

Net effect was correct (builder confirmed the dirty state matched
oracle's report exactly, then re-ran the build to confirm
idempotency before committing). But the verification step was extra
work caused by oracle leaving cleanup undone. Read-only personas
should leave the tree as they found it.

**Suggested mitigation:** one-line addition to `personas/oracle.md`
(and any other read-only persona that occasionally executes
build/measurement commands — `redteam`, `inspector`, `analyst`,
`profiler`, `investigator`) — *"if you execute a build/measurement
command that modifies tracked files, restore them with `git
checkout -- <paths>` before returning. Read-only personas should
leave the working tree as they found it."* Pure prompt nudge.

## v0.10 Slice 3 follow-ups (2026-05-20)

### `/conductor gc` autocomplete-but-not-wired

Surfaced during slice-4 implementation (`4734298`). The `gc` token
appears in the `SUBCOMMANDS` array in `src/commands.ts` and is offered
by pi's slash autocomplete, but the command's dispatch switch has no
`case "gc":` arm — typing `/conductor gc` falls through to the
`unknown subcommand` path. `runGcCmd` is exported, fully tested, and
reachable via `/conductor`'s sibling subcommands' code paths; only the
wire-up is missing. Pre-existing v0.9 bug (the gc subcommand was added
in `e0fb122` and the dispatch switch was last edited around the same
window).

Fix size: 1-line `case "gc": return runGcCmd(args, ctx);` plus a
regression test in `tests/commands-gc.test.ts` (or wherever
`/conductor gc` is exercised today) that pins `dispatch("gc", ...)`
resolves to `runGcCmd`. ≤30 LOC.

### Watchdog default soft threshold tuning from field data

Observed during slices 3–4: legitimate slow operations (npm test 3x
stability loops, foreground sub-agents on long LLM turns) trip the
120s soft advisory. **No false escalations to hard observed yet** —
the advisory layer is doing its informational job; the cry-wolf risk
is purely about advisory frequency, not about wrongful kills.

Track-level recommendation: leave defaults until ≥10 advisory firings
are logged across personas + workloads, then revisit. Signal sources:
`<sub-agent-stalled>` envelopes in parent transcripts, the `STALLED`
widget glyph (slice 4), `/conductor watchdog status` output. Reference
`docs/v0.10-watchdog-design.md` §7 R1.

When reopened, candidate adjustments:
  1. Bump conductor-default soft from 120s → 180s or 240s.
  2. Land the deferred persona-frontmatter watchdog overrides (see
     entry below) so personas with reliably-slow tool calls (e.g. a
     hypothetical `deployer`) can carry their own soft threshold
     without hand-coded per-spawn overrides.
  3. Both 1 and 2 — global default tuned conservatively, persona
     overrides handle the long-tail.

### Persona-frontmatter watchdog overrides — DEFERRED

Slice 3 of v0.10 plumbed `kill_on_stall` and `stall_threshold_seconds`
as per-spawn LLM tool args (and `ConductorConfig` defaults) but did
not extend `PersonaOverride` in `src/personas.ts` to carry them. The
cascade in `docs/v0.10-watchdog-design.md` section 6 row 3 reads
"per-spawn arg > project config > user config > built-in default"
and does not enumerate the persona-frontmatter layer, so the
omission is in scope.

Use case to wait for: a persona that should default to
`kill_on_stall: true` (e.g. a fully-autonomous chain operator) or a
persona whose tool calls are reliably slow enough that the soft
threshold should default higher (e.g. a `deployer` persona that
runs CDK synth + deploy). Until either appears, the per-spawn arg
is sufficient.

When reopened, mirror the `timeoutMinutes` cascade pattern (already
established in v0.8.3). Reference:
`docs/v0.10-watchdog-design.md` section 7 "Deferred from Slice 3".
