# v0.8.2 backlog

Seeded from v0.8.1 Item 1 close (commit `423f500`, finalizer `finalizer-7794` ruling 2026-05-15).

## P0 — workflow defects

### Parent-reload contract for context-filter / runs changes

The pi-conductor extension loads from `./src/index.ts` at parent-session
startup with no live-reload mechanism. This means iterating on
`src/context-filter.ts`, `src/runs.ts`, or any other extension surface
requires restarting the parent pi session — losing scratchpad state,
in-flight sub-agent records, and the active conductor brief. v0.8.1 Item 1's
§8.3 manual smoke gate could not run in-session for this reason; the slice
shipped on static evidence (mutation test + byte-identical sentinel + 9 new
green tests + out-of-band `tsx` smoke) instead.

Decide one of:

- (a) Document the reload contract for contributors (acceptable but a tax).
- (b) Build a reload-on-disk-change mechanism for the extension.
- (c) Provide a `pnpm dev`-style watch flow that auto-restarts the parent.

Triage in early v0.8.2.

## v0.8.1 Item 1 follow-ups (deferred)

### §8.3 manual smoke gate — execute in next conductor session

Per design `docs/v0.8.1-item1-design.md` §8.3: re-run the witnessed dogfood
scenarios (a `critic` brief and an `oracle` brief whose parent's filtered
slice contained orchestration prose) on the post-`423f500` code. Confirm the
personas execute their briefs without meta-commentary. Record evidence in
this backlog. **This was not run in the originating session due to the
parent-reload constraint above.**

### Inverted comment at `tests/context-filter.test.ts:479`

Comment claims preserving the non-excluded toolCall would create a dangling
toolResult orphan; the actual chosen invariant (drop the whole turn) is the
inverse — dropping orphans the surviving toolResult of the non-excluded
call. Runtime is correct; only the comment's reasoning is inverted.
~3-line edit.

### Stale `stopReason: "toolUse"` on `dropThinking`-only rewrite path

TODO marker landed at `src/context-filter.ts` (the `// TODO(v0.8.2)` line).
Functional fix deferred. Latent — no current consumer of filtered slices
reads `stopReason`.

### §11 verifier-brief sub-rule

Per `docs/v0.8.1-item1-design.md` Q-followup block: §11 should mandate that
verifier briefs include the claimed change explicitly, since `verifier`
flipped to `inherit_context: none` and now boots fresh. Small
`src/conductor-prompt.ts` + `tests/conductor-prompt.test.ts` slice.

## Other v0.8.1 items still open (not started)

- **Item 2** — parallel write-builder git collision (mitigation (a) shipped;
  (b) builder SHA-check guard and (c) harness-side cap still open).
- **Item 3** — foreground transcript view UX (8 documented issues; large
  slice, needs `inspector` → `designer` chain).
- **Item 4** — no-substantive-final-message harness guard (small slice).
- **Item 5** — context-inflation compaction (medium slice).

See `docs/v0.8.1-handoff-context.md` for full descriptions.

## Pre-existing drift (not v0.8.1 Item 1, but flagged for visibility)

`PRD.md:561` claims "ON by default" — v0.8 shipped OFF default in commit
`9becc65`. The PRD line is stale. Out of scope for v0.8.1 Item 1; flag for
whoever next edits the PRD locked-decisions section.
