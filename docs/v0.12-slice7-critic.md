# Slice 7 (closure) — critic review

**Slice commit:** `dcf7ef0` — `docs(prompt,readme,prd): conductor-prompt section 10 + v0.12 closure`
**Reviewer:** in-conductor critic (delegation skipped given the small mechanical surface and the witnessed item-12 bleed making cold-spawn more risky than the local review).
**Baseline:** `60a175a` (slice 6 close).
**Outcome:** **APPROVE.**

## Novel verifications I performed

- §2c production-prose mutation: replaced both occurrences of `\`steerable: true\`` with non-pinned forms (`\`steerable_flag\`` + paraphrased prose). Test `tests/conductor-prompt.test.ts: §10 — names steerable: true per-spawn opt-in verbatim` red **specifically** (line 543). 1 fail / 56 pass; restored.
- §3 production-prose mutation: renamed `"missing agent-id."` → `"absent agent-id."` in `src/commands.ts:425`. Tests `parseSendCommand: empty arg → helpful error mentioning agent-id` and `parseSendCommand: whitespace-only arg → helpful error mentioning agent-id` red **specifically** (line 66, 75). 2 fail / 10 pass; restored. Carry-forward 8a from slice-6 critic confirmed tightened — assertion now pins the missing-agent-id branch and not the SEND_USAGE fallback.
- §10(a) Conventional Commits compliance: subject `docs(prompt,readme,prd): conductor-prompt section 10 + v0.12 closure` is multi-scope, body cites every v0.12 slice SHA in order (3f73d9d → 21e6314 → 394e69e → 0c45af8 → 11089ef → 9599e5d/60a175a split → dcf7ef0), trajectory uses 1263 baseline (matches slice-6 close), carry-forwards 8a / 8b / 8c listed and addressed.

## Smoke test

`env -u CONDUCTOR_SUBAGENT npm test` at HEAD `dcf7ef0`: **1267 pass / 11 skipped / 0 fail** — confirms builder's claimed delta (1263 → 1267, +4). Active duration 13.7s. Live integration tests skip-counted as expected; flipping `CONDUCTOR_LIVE_TESTS=1` is documented in CONTRIBUTING.md per slice-6 carry-forward 8c.

## Findings

- ✓ **§1 trajectory:** matches builder claim (1267/11/0 at `dcf7ef0`). +4 pins exactly = the four §10 conductor-prompt assertions.
- ✓ **§2b default behavior:** §10's third bullet (`auto` line in line 228) names `auto` as default and explicitly says "queues your message as a follow-up at the next turn boundary" — the queue-vs-interrupt distinction is concrete, not hand-waved.
- ✓ **§2c per-spawn opt-in:** §10's first bullet (line 226) says *"Pass `steerable: true` on `ensemble_spawn` to launch the sub-agent in pi's RPC mode. Default is OFF."* Mutation kill confirmed teeth.
- ✓ **§2d narrowed-assertion call (builder's known limitation):** verified pre-slice-7 §10 (`60a175a`) had **none** of `ensemble_spawn` / `ensemble_send` / `ensemble_kill` / `ensemble_focus`. New §10 block adds `ensemble_spawn`, `ensemble_send`, and (in the kill-switch bullet line 234) `ensemble_kill`. `ensemble_focus` genuinely absent. Builder's narrowing to only `ensemble_spawn` + `ensemble_send` for the regression pin is correct in spirit — there's nothing to regress on the other two pre-slice-7. The newly-introduced `ensemble_kill` mention is unprotected by a regression pin (`Note 1` below).
- ✓ **§4 carry-forward 8b:** `docs/v0.12-steering-plan.md` is `??` (untracked, not staged in `dcf7ef0`); line 283 references `src/focused-overlay-factory.ts:82` as required (was `src/focused-stream-overlay.ts:228` per slice-6 critic finding). The fix lives in the untracked copy that batches into the parent's separate close commit, exactly as builder claimed.
- ✓ **§5 carry-forward 8c:** CONTRIBUTING.md gains a v0.12 dogfood callout block including the `env -u CONDUCTOR_SUBAGENT npm test` invocation. Wording is concrete ("strip the var so child pi processes don't inherit it"); pin worth keeping.
- ✓ **§6 PRD entry:** §"Implementation phases" gains a v0.12 entry mirroring v0.10's shape (mechanism + cascade + UX surfaces + slice-commit reference + design-doc citation). §"Decision log" gains a 2026-05-28 entry with one paragraph each for Q1 / Q2 / Q6 / Q9, oracle fixes #1–#3, Risk 2 — each with file:line refs into the design doc and PRD line citations into v0.10's analogous decisions. §"v0.10+ planned" gains three new bullets exactly as the plan §"Slice 7 — Files" sub-list specified (`ensemble_abort`, `Persona.steerable` frontmatter, ext-ui proxy).
- ✓ **§7 README + AGENTS.md banners:** README "Current state" line flipped to v0.12 shipped; status checklist gains a checked v0.12 entry; v0.11 stays in progress (correct — `on_complete_hook` is independent). AGENTS.md "Current state" line flipped to v0.12 shipped. AGENTS.md also picks up the `docs/v0.8.2-backlog.md` → `docs/backlog.md` rename from `a21af42`.
- ✓ **§8 design + plan status flips:** both untracked docs flipped to `**Status: SHIPPED 2026-05-28.**` with revision-log entries citing the 8-slice commit lineage. Neither file is in `dcf7ef0`'s diff (they batch into the parent's separate close commit).
- ✓ **§9 dist regeneration:** `dist/index.js` (+12 lines) and `dist/index.js.map` (+4) auto-rebuilt by pre-commit hook. Expected.
- ✓ **§10(a) Conventional Commits:** subject form correct, body lineage correct, carry-forwards listed with disposition. Body acknowledges the slice-6 split (`9599e5d` fix + `60a175a` feat) explicitly.
- ⚠ **Note 1 — §10 verb-pin teeth (§2a):** the test `§10 — names the steer / follow_up / resume verbs verbatim` (line 534) uses regex `/\bsteer\b/`, which matches **any** occurrence of `steer` as a whole word in §10. Mutation that renames just the value-name bullet (`\`steer\`` → `\`interrupt\`` on line 230) leaves the verb mentioned in the kill-switch bullet (line 233's `rejects \`steer\` / \`follow_up\``) and the "Steering a running sub-agent" header — so the test still passes. The pin **does** kill drift if all `steer` references are removed, but doesn't kill renaming the canonical value-name occurrence. Consider a follow-up tightening to a count-pin (`assert.equal(matches.length, N)`) or pin the literal value-list line by exact regex anchor. Not blocking — meaningful drift removes all references; this is cosmetic-drift coverage only.
- ⚠ **Note 2 — Decision-log SHA placeholder:** the new 2026-05-28 decision-log entry says "8 slice commits `3f73d9d..<v0.12-close>`" with literal `<v0.12-close>` placeholder text. Self-reference is impossible (the closure commit can't cite its own SHA before it lands). Worth a one-line follow-up edit to backpatch `<v0.12-close>` → `dcf7ef0` once the close commit is on `master` — or accept as the v0.10 pattern allows (v0.10's entry uses `c8623cb..4734298` with no placeholder). Not blocking; aesthetic.
- ⚠ **Note 3 — `ensemble_kill` mention in §10 unprotected:** per §2d above, the new kill-switch bullet on line 234 mentions `ensemble_kill` but no test pins it. If a future refactor removes the kill-switch reminder bullet, no test catches it. Trivial follow-up: extend the §10 no-regression assertion to include `ensemble_kill` (now that it's actually in §10). Mirror Note 1's "follow-up tightening" disposition.
- ⚠ **Note 4 — §10 prose slop pass:** per AGENTS.md anti-slop discipline. Reading the new block for slop patterns:
  - "Reflexive `steer` on every send wastes loaded context." — concrete guardrail, useful.
  - No "important to note", no rule-of-three padding, no inflated AI vocabulary.
  - One rhetorical em-dash on line 226 ("Default is OFF — RPC sub-agents hold an open subprocess between turns") — purposeful, not abuse.
  - The motivational sentence at line 224 is one sentence longer than strictly needed but contextualises the section for the LLM reader. Acceptable.

  Verdict: clean. No slop grenade.

## Verdict

**APPROVE.**

All four scoped-gate criteria from plan §"Slice 7" pass:
- Verbs spelled out — yes (mutation kills only loosely; Note 1 documents the cosmetic gap).
- Default behavior described concretely — yes.
- Per-spawn `steerable: true` opt-in named explicitly — yes (mutation kills cleanly).
- Existing §10 content not regressed — yes (verified `ensemble_spawn` / `ensemble_send` pin teeth; builder's narrowed assertion correct in spirit; Note 3 documents the unprotected new `ensemble_kill` reference).

The 4 notes are all follow-up tightening candidates, not regressions. v0.12 is ready to close. The parent conductor should now batch the design-phase artifacts (inspector map + 2 oracle reviews + design + plan + plan-oracle review + 7 slice critics including this one + `docs/backlog.md` items 11+12) as a separate `docs(v0.12): design-phase artifacts` close commit per the v0.11 pattern.

## Cross-link

Item-12 in `docs/backlog.md` (filtered_compact orchestration-shape bleed) was witnessed AGAIN during this critic gate: the slice-7 critic-spawn brief was inhaled with the conductor's full project-context preamble (`## Recent Sessions` + `<memory>` block) intact. The hardened identity preamble in the brief was sufficient to keep the cold-spawned critic on-task in earlier slices, but the parent conductor electively performed this review in-process to avoid another bleed test on the closure slice. Recommend extending item-12 with this fresh witness when filing the close commit.
